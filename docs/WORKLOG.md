# Mealboard 작업 로그 (WORKLOG)

> 작업 이력·세션 인계의 단일 보관소(최신이 위, yyyy-mm 절). **README 는 프로젝트 소개 전용,
> CLAUDE.md 는 작업 규칙·설계 결정·현재 상태 요약 전용** — 지난 세션의 서사는 전부 여기로 (09-12 사용자 결정).

## 작업 로그


### 2026-09
- 09-21 **λ선 정지 사고 → 발 잘림 투영 보정(수동 실측 3차)** — ① 월 점심 20분째 '배식 시작 대기': 09-18 편집기에서 ROI 아래변(=λ선)을 화면 바닥(y=1.0)에 붙인 탓 — 발끝은 프레임 밖으로 못 나가 부호 변화가 불가능, λ 영구 0. 사용자에게 '아래변을 바닥까지' 안내했던 내 실수. 아래변 y≈0.91 로 올려(사용자) 3분 만에 복구. **교훈: λ선은 화면 경계에 붙이면 죽는다.** ② tracks 모드는 좌석 식사 인원까지 세어 대기 10분+ 과대(사용자 확인) → `QUEUE_SOURCE=roi` 복귀 + `DWELL_BIAS=1.0` 리셋(1.4 는 옛 기하 기준). ③ roi 복귀 후에도 L 0~1: **수동 실측 3차(12:33, 랩 8건 = 95·75·20·66·146·47·53·16초, 중앙값 ~60초)** 로 실제 줄 3~4명 확인 — 발이 프레임 하단에 잘린 트랙(v≈1)이 아래변(y≈0.91) 밖으로 새는 구조. 처방: counter.py 가 잘린 트랙의 발끝을 λ선 바로 안쪽으로 투영해 **멤버십만** 재판정(통과 판정은 원좌표). dwell 진입도 같이 살아난다 — bias 재보정은 이 수정 뒤 실측 쌍으로. ssh 별칭 mbcf→`mb-cf`(사용자 정리). ④ **λ 측정선 분리(사용자 설계)**: 편집기에서 λ 변을 지정하면 측정선이 그 변에서 **안쪽으로 평행 생성**(`roi.lambda_inset` 기본 0.04·`lambda_scale` 길이 배율, 슬라이더 2개) — ROI 폴리곤을 화면 바닥까지 붙여도 λ 가 산다. `vision/zones.lambda_line`(순수·테스트 5) = 편집기 `lamLine` 동일 기하, counter 는 파생선으로 LineCounter. 기존 파일은 inset 없음 = 변 그대로(하위 호환). sw/UI v39. ⑤ **끼니 토글 복귀(사용자 결정, 09-18 '토글 없음' 번복)**: 낮에 석식 데이터를 볼 길이 없었다 — 대기 화면 5카드에 토글 복원(단일 격자 히트맵 + #heatseg 포함), 자동 전환은 09-16 규칙(TOUCHED 안 된 토글만 경계에서 따라옴). sw/UI v40.
- 09-18 **대기시간 화면 개편 — 끼니 자동 표시·'지금' 타일 점멸·추이 툴팁(사용자 요청)** — ① "어제 목 석식 히트맵 비었다" 문의: 수집 정상(415표본 17:00~18:09), 원인은 또 14:10 하루 1회 집계 타이밍(09-16 수요일과 동일). 타이머 추가 대신 실시간성 표현으로 방향 전환. ② 대기시간 화면 인사이트 카드(히트맵·황금·예보·병목·품질)는 **토글 폐기, 현재 끼니(defaultMeal) 자동 표시만** — 히트맵 2열(`.heat2`)도 단일 격자로, 경계(14시·21시)에서 `followMeal` 이 카드 전체 전환(두 끼니 캐시 유지). 끼니 토글은 영양추이·TOP5 만. ③ 히트맵 '지금' 타일: 오늘 요일·현재 시각 셀에 `data-now`(CSS 2.2초 점멸 + 파선 테두리, 모션 축소 시 정지), 빈 셀에도 wd·min 을 남겨 마커가 앉는다. 30초 poll 의 `markHeatNow` 가 이동. ④ 추이 막대 호버·탭 툴팁 `#charttip`(drawChart 가 남긴 chartGeo 로 픽셀→분 역산, 시각·오늘 대기·평소 대기) — 과거 이력 확인용. 함정: `transform: translate(-50%)` 가 임베디드 미리보기에서 적용되지 않아 좌표·클램프를 JS 로 직접. ⑤ 학교 망이 Cloudflare 엣지 IP 하나(104.21.70.130)만 차단해 `ssh mbcf`·curl 이 죽던 것 — hosts 에 172.67.223.145 고정 처방(브라우저는 IP 폴백이 빨라 멀쩡했던 것; 이후 재시도에서 hosts 없이도 붙어 간헐 차단으로 판단, hosts 는 예비). ⑥ 점심 실사 중 "트랙 10명인데 대기 1명" — ROI 가 실제 줄보다 좁아 L 이 샘. **공표 L 재료를 화면 안 트랙 전체로**(`.env QUEUE_SOURCE=tracks` 기본, roi 로 회귀 가능, 같은 25초 중앙값 평활) — 카메라가 배식대 구석이라 화면 대부분이 줄이라는 전제, 앉은 인원 오검이 크면 roi 복귀 또는 ROI 재조정. sw/UI v37, 테스트 192. ⑦ **수동 실측(A) 2차, 금 12:31 출입구→배식대**: lab1 2:41 · lab2 2:29 · lab3 4:24 · lab4 2:04 · lab5 0:59 — **중앙값 2.48분**. 같은 창 시스템은 공표 0~1.5분·L 0~2명(ROI 누수 재확인), `measured_wait_min`·`calib` 전부 NULL — **DwellTracker 이벤트 0건**(ROI 진입→λ선 통과로 이어지는 트랙이 없음) → DWELL_BIAS 는 곱할 대상이 없어 1.4 유지, 처방은 QUEUE_SOURCE=tracks(트랙 ~10명 ÷ λ 4/분 ≈ 2.5분 = 실측과 일치 예상). 남은 일: 재시작 후 실측 대조, ROI 를 줄 전체로 재조정하면 dwell 실측·K 도 살아난다(아래변 두 꼭짓점을 화면 바닥까지 — 발 잘린 인원의 foot y≈1.0 이 ROI 밖으로 새던 원인, 왼변 난간선은 유지). vision 14:09 재시작으로 tracks 적용. ⑧ **히트맵 오늘 행 실황(사용자 요청)**: "어제 목 석식이 안 보인다" 2건 모두 집계는 정상·표시가 하루 늦는 구조 문제 → 오늘 요일 행은 4주 평균 대신 queue.db 즉석 5분 빈(`live_today`), 프론트 히트맵 폴링 30분→5분(fastInsights), 리드에서 '지금은 ○○ 시간대' 제거. 학교망 Cloudflare 엣지 104.21.70.130 차단 확진(172.67 은 302) — hosts 고정(관리자 권한, 사용자 실행). ⑨ **배포 잔류의 진짜 뿌리**: main.py 가 js/css 에 no-cache 를 붙여도 **Cloudflare Browser Cache TTL 기본 4시간이 max-age=14400 으로 덮어씀** → 대시보드 Caching → Browser Cache TTL 을 'Respect Existing Headers' 로(사용자 변경, 적용 확인 — 이후 응답 no-cache·REVALIDATED). 이제 배포 즉시 반영, sw CACHE 버전은 이중 안전장치로 유지. sw/UI v38.
- 09-17 **낮 — 표본 평활·λ 3분·'여유' 절대 기준(사용자 요청)** — 중식 실사 대조에서 공개 화면 0초 vs 관리 스트림 6분(L 이 프레임마다 0↔3↔7 깜빡임), 대기 널뜀 확인. 원인 셋: 원거리 검출 끊김(트랙 ID 계속 재발급), λ 2분 창의 0.5명/분 계단, 표본 무평활. 처방: ① `MedianWindow`(counting.py) — 공표 L = 25초 중앙값, 공표 대기 = 90초 중앙값(state ok 표본만; 원시는 wait_raw_min 열에 그대로 — K 보정 분모도 여전히 원시) ② `RATE_WINDOW_SEC` 기본 120→180(계단 1/3명/분) ③ '여유·바로 가세요'는 상대 위치와 무관하게 **절대 30초 이하**(사용자: "학교 급식에서 1분 넘으면 여유가 아니다" → 같은 날 30초로 더 조임 — '여유' 보고 갔는데 밀려 있는 것보다 뜻밖의 여유가 낫다), 혼잡은 상대 상위⅓(폴백 12분) 유지. 창 전환 시 평활 창도 리셋. 테스트 191(중앙값 2건 추가), check_manual 0, sw/UI v35. L 과소 계수 자체(66° 화각·원거리)는 Wide 카메라·ROI 재확인이 근본 대책으로 별도. **오후 — 첫 수동 실측(A) 3값 대조**: 사용자 12:44 진입→12:46 도착 = 실제 1.3분 vs 그 순간 원시 0분(L=0 깜빡임) vs 자동 체류 중앙값 0.9분. 결론: 오차는 배율이 아니라 L 누락 → K 곱셈으론 못 살림(0×K=0), 자동 실측은 방향·자릿수 일치(트랙 끊김으로 약 30% 과소). 처방으로 **공표 하한** 추가: L≥1 이고 공표가 체류 중앙값보다 낮으면 체류 중앙값으로 올림. '여유' 절대 상한도 1분→30초(뜻밖의 여유가 낫다, sw v36). 체류 과소 편향엔 `.env DWELL_BIAS`(기본 1.0) 신설 — Pi 는 오늘 대조(0.9 vs 1.3)로 **1.4**, 이후 수동 실측이 쌓이면 조정(사용자 결정). 20초 문턱은 날것 기준.
- 09-16 **밤 — 자동 실측·보정 B안 + 상대 판정 + 급식 끝 시각 조정(사용자 요청)** — ① 대기 과소 체감의 대책으로 `DwellTracker`(counting.py): ROI 진입 시각·그 순간 예측을 기억, λ선 통과 때 체류 이벤트(20초 미만·진입 미확인은 버림) → 이동 창(15분) 중앙값과 보정계수 K=median(실측/예측, 0.5~3.0, 5이벤트↑). counter 는 공표 대기 = 원시×K 로 기록, samples 에 wait_raw_min·measured_wait_min·calib 열 ALTER 추가(개인 값 저장 없음). ② 여유/보통/혼잡 판정을 그 창의 오늘 공표 대기 min~max 상대 위치로(status `wait_range`, 30표본·폭2분 문턱, 폴백 고정 5/12분), 히어로 각주에 ×K·범위 표기. 수동 실측(진입·도착 기록) 병행 합의 — 원시/실측/보정 3값 대조용. ③ 수집·집계 창 끝을 실측으로 조정: 중식 13:40·석식 18:10(MEAL_WINDOWS·LUNCH_END·DINNER_END 기본값 + Pi .env). ④ 낮에 "수요일 석식 히트맵 없음" 문의 — 수집은 정상(534표본), 원인은 14:10 하루 1회 집계 타이밍(수동 집계로 채움; 타이머 2회 제안은 보류). 테스트 189, sw v34.
- 09-16 **중식/석식 지표 분리(사용자 요청)** — 지금까지 모든 인사이트(히트맵·황금·예보·병목·품질·영양추이·TOP5)와 영양 지표가 중식 전용이었고 석식 표본(09-11부터 실측)은 `ROLLUP_WINDOW=lunch` 창 밖으로 버려지고 있었다. 사용자 확인 3건: ① insights.db 에 meal 열(파일 분리 대신) ② 카드 안 끼니 토글(카드 2배 대신) ③ 석식 영양 지표도 산출(중식과 같은 '한 끼 권장량' 기준 — 그간 '영양 교사 판단'으로 보류하던 항목 확정). 백엔드: 스키마 v2(전 표 PK 에 meal, menu_stats·인기 기준값 끼니별, meta 는 '한 파일 = 한 창 모드·한 출처'로 완화, v1 은 백업 후 `--all` 재집계), `agg_meals()` 로 rollup 이 중식·석식 두 창을 각각 집계(그 끼니 창에 표본 0 이면 행 없음), `.env DINNER_START/END`, `/api/insight/*` 7종에 `?meal=`, `fetch_neis` 가 석식 assess·carbon 산출, `mealjson` 이 석식 행. 프론트: 히트맵 중식·석식 2열(`.heat2`, 열 상한 18, 한쪽만 있으면 `.solo`), 나머지 6카드는 오른쪽 위 끼니 토글(`core.mealSeg`, 기본 14시 전 중식·후 석식, 두 끼니 캐시로 전환 즉시), 오늘급식 석식 지표 한 줄(`.mstats`)·탄소 중식+석식 합산, sw v30. 함정 재확인: 화면 모듈 최상위에서 core 함수(defaultMeal)를 부르면 순환 import TDZ — 헤드리스가 아니라 미리보기 실기동에서 잡았다(콘솔 오류 → mount() 로 이동); 미리보기 탭의 모듈 맵은 재로드로도 안 비워져 새 탭으로 검증. 테스트 184, check_manual 0 불일치. Pi 반영 시: insights.db `.bak` 후 `--all` 재집계 + `fetch_neis` 1회 + api·admin 재시작.
- 09-12 **저녁 — 운영 구조 롤백(사용자 결정)** — 낮에 도입한 'Pi 워크트리 직접 편집·커밋·push' 방식을 폐기하고 기존 구조로 복귀: 개발 메인은 로컬 PC 작업 폴더(다른 PC 는 Claude Code 원격제어로 접속), 배포는 로컬 커밋 → push → GitHub → Pi `git pull --ff-only`, Pi 는 배포 전용(직접 수정 금지, 작업트리 clean 유지). 점검 결과 잔재는 CLAUDE.md 문구 4곳(§0·§5 허용·§5 금지·§6)뿐이라 원복했고, git remote 는 origin(GitHub) 하나·Pi 워크트리도 clean·동일 커밋이라 정리할 것이 없었다. 배포는 `deploy/deploy_pi.sh` 신설(clean 검사 → `git pull --ff-only` → pyproject/uv.lock 변경 시 `uv sync --extra vision` → 유닛 변경 시 `setup_pi.sh` → api·admin 재시작, vision 은 `--with-vision` 플래그로만 → `/api/status` 200 검증). 로컬 PC 가 behind 22 였던 것도 ff 로 동기화.
- 09-12 **문서 구조 개정(사용자 결정)** — README 는 프로젝트 소개 전용, 작업 이력·세션 인계는 이 파일(docs/WORKLOG.md)로 일원화, CLAUDE.md §0 은 규칙·현재 상태 요약만(지난 세션 인계 4건을 여기 '세션 인계 이력' 절로 이관, 원격 접속·하드웨어·작업 스타일·hailo-ollama 는 압축 불릿으로 유지). 같은 지침을 사용자 전역 CLAUDE.md 에도 등록(전 저장소 공통).
- 09-12 **오전 — 유출 커밋 이력 교체 + 급식 없는 날 이슈피드·전환 페이드·주간식단 폰트(사용자 요청)** — ① 어제 커밋 `012677e` 가 `git add -A` 로 `.env.bak-*` 3개(터널 토큰·API 키)·`hailort.log`·`qr.png` 를 쓸어담아 push 된 것을 발견 → HEAD 하나뿐이라 soft reset 으로 news.js 수정만 남겨 재커밋(`07d9036`), `--force-with-lease` 로 원격 이력 교체, `.gitignore` 에 `.env.bak-*`·`*.log`·`qr.png` 추가. **노출된 키(CF 터널 토큰·DeepL·Access AUD)는 교체 권장**. ② `/api/status feed.meal_day`(NEIS 캐시에 오늘 메뉴가 있는지) — 시작 화면·자동 전환이 창(시각)만 보던 것을 급식 있는 날로 좁혀 주말·방학 낮에도 이슈피드로 시작. ③ 자동 전환은 화면 전체 `xfade` .6s(모션 축소면 생략). ④ 주간식단 요일 칩·중식/석식 라벨·메뉴·kcal 폰트 확대(기본 +2px 내외, 데스크톱 한 단계 더). sw v29.
- 09-11 **밤 2 — 이슈피드 두 섹션·워드클라우드·급식 시간 자동 전환·다음 급식 날짜(사용자 요청, Pi 미반영)** — ① 띠의 "다음 급식 … (내일)" 이 토요일 밤에 틀렸다 → `app/lunch.next_meal_day`(NEIS `meal.json` 의 급식 있는 날 우선, 범위 밖은 주말만 건너뜀) + `status.next_with_day` 가 날짜·요일을 주고 화면은 "(월요일)" 로, 못 정하면 문구 생략(테스트 2). ② 해시 없이 열면 급식 창 밖에는 이슈피드로 시작, 창이 열리는 순간 대기시간으로 한 번 전환(`core.boot`·`autoSwitch`, 사용자가 탭을 누른 뒤엔 개입 없음). ③ `news_feeds.json` 에 `category`(climate/tech), `fetch_news.build` 는 섹션별 `collect(category)` → `sections[]` + `keywords`(제목·요약 낱말 빈도, 불용어 제거) + 호환 `items`. tech 피드는 curl·XML 로 검증한 넷만. 화면: 기후(차콜·멜론 선)·기술(틸 잉크·아쿠아 선) 두 카드, 데스크톱은 나란히 6+6컬럼, 아래 '오늘의 키워드' 워드클라우드(빈도 5단 크기, 색은 출처: 기후 멜론·기술 틸·둘 다 차콜, 해시 셔플로 큰 낱말이 앞에 몰리지 않게). sw v28, 테스트 181. **Pi 반영은 네트워크 사정으로 보류** — 집 PC 세션에서 `git pull` + api·news 재시작 + `fetch_news` 1회.
- 09-11 **밤 — 더미 정리·문구·배포 잔류 대책(사용자 요청)** — ① `jobs/purge_mock.py`(세 겹: 빈 조건 거부·자동 `.bak`·되돌리기 명령, VACUUM)로 Pi `queue.db` 의 실측 시작(17:35) 이전 165만 행을 걷어냈다(200 MB → 168 KB, 백업 보존; 더미가 다시 필요하면 mock 유닛 + `FEED_SOURCE=mock`). ② 주간식단 요일·날짜를 흰 알약 칩으로 분리, 영양 지표는 **중식 한 끼 기준**임을 각주로(석식 합산은 영양 교사 판단 사항). ③ 인사이트 빈 상태 문구를 '급식 시간 데이터가 쌓이면 반영됩니다'(오늘 즉석)·'일정 기간 데이터 수집 후 반영됩니다'(집계) 로 — API reason·셸·`core.why` 셋 다. ④ **배포 뒤 옛 모듈이 남는 문제** 두 갈래로 해결: 관리 UI 는 `/admin-ui/` `no-store` + `import ?v=UI_VERSION`(정적 import 를 동적으로), 학생 화면은 새 서비스워커가 탭을 넘겨받을 때 한 번 자동 재로드(`controllerchange`). `core.UI_VERSION` 과 `sw.js CACHE` 를 같이 올린다(v27). 함정: 문구 치환을 sed 정규식으로 하다 한 줄이 빗나가 옛 문구가 되돌아왔다 — 문자열 치환은 정확 일치 편집으로.
- 09-11 **저녁 — 석식 반영·오늘급식 2열·편집기 직관성·초기화·수집 창 앞당김(사용자 요청)** — `fetch_neis` 가 석식(MMEAL_SC_CODE 3)도 받아 `week[].dinner` 로(실패해도 중식은 나간다), 주간식단 칩 중식/석식 2행, 오늘급식 중식·석식 2열 + 알레르기 경고 띠(내 알레르기가 든 메뉴 이름을 한 줄로) + 칩 정렬. 편집기: 칩을 작업 순서(①프레임 ②ROI·λ ③보정)로, λ 변 hover 굵게, 출구 화살표를 변에 수직으로 + 출구 쪽 띠 + 라벨(변을 따라 그리던 화살표가 '오른쪽으로' 로 읽히던 문제), 보정 4점 청록 파선·m 라벨·자동 진행·점 클릭으로 행 선택, 저장 버튼 이름에 대상 표기, **템플릿으로 초기화**(`zones_store.reset` → `.bak`, 감사 `zones.reset`, 테스트). 수집 창 시작을 중식 11:20·석식 17:00 으로(끝은 그대로, `.env MEAL_WINDOWS` 기본값·테스트). sw v23, 테스트 178.
- 09-11 **실데이터 가동(석식 17:35) + venv 함정 수정** — 학교 PC 에서 세 경로(Tailscale·`ssh.kjhs-meal.com`·`admin.`)가 모두 막혀 휴대폰 핫스팟으로 옮긴 뒤 `ssh mbcf`(cloudflared ProxyCommand, 이 PC 에도 설치)로 붙었다. `.env` 는 이미 맥북 세션이 `FEED_SOURCE=vision`·`ROLLUP_WINDOW=lunch` 로 바꿔 두었고 보정 4점(x 0/2.06, y 4/21.15 m)·ROI·λ 변도 저장돼 있었는데 **vision 이 `ModuleNotFoundError: ultralytics` 로 재시작 루프** — 16:03 의 `uv sync`(pyjwt 추가)가 pyproject 에 없던 ultralytics·torch·hailort 를 venv 에서 지운 것(cv2 도 시스템 4.10 으로 후퇴). venv 에 torch 2.14·torchvision 0.29·ultralytics 8.4.147·hailort 5.3.0 휠을 다시 넣고 재시작 → 17:35 부터 `[석식] 실측 기록`, `/api/status source=vision live=true`. 재발 방지: `pyproject.toml` `[project.optional-dependencies] vision`(ultralytics·torch·torchvision·opencv-python) + `uv lock`, `setup_pi.sh` 는 `uv sync --extra vision` + `data/models/hailort-*.whl` 설치. **Pi 에서 앞으로 `uv sync` 는 반드시 `--extra vision`**.
- 09-11 **관리 앱 외부 접속 — `admin.kjhs-meal.com` + Cloudflare Access(사용자 결정)** — 학교 망이 Tailscale 컨트롤플레인뿐 아니라 DERP 릴레이도 막아, 밖에서의 직결은 NAT 구멍이 살아 있는 동안만 통한다(14:40 됐다가 14:57 끊김). '급식실에 가야 관리 앱이 열린다' 는 말이 안 되므로 같은 Cloudflare 터널에 관리 경로를 두고 앞단에 Access(이메일 일회용 코드)를 세운다. 관리 앱은 `Cf-Access-Jwt-Assertion` 을 팀 공개키(JWKS)로 검증해 이메일을 `ADMIN_USERS` 와 대조(`app/admin/access.py`, `pyjwt[crypto]`); **Cloudflare 를 거쳐 온 요청(`cf-ray`)에서는 Tailscale 헤더·로컬 키를 보지 않는다**(터널로 외부 트래픽이 8101 에 닿으면 위조 헤더가 실제 위협). `.env CF_ACCESS_TEAM`·`CF_ACCESS_AUD`(비면 닫힘). 테스트 20건(위조 서명·alg=none·aud·iss·만료). 매뉴얼 9-5 에 대시보드 순서. **16:15 가동**: Access 앱 하나에 `admin`·`ssh` 두 대상, 터널 경로 둘(HTTP 8101 · SSH 22), Pi `.env` 반영 뒤 토큰으로 `whoami` → `via: access` 확인, 토큰 없이는 302. SSH 는 `ssh mbcf`(cloudflared ProxyCommand) 로 학교 망과 무관하게. 함정: 학교 안 일부 Wi‑Fi 가 새 하위 도메인의 TLS(SNI)를 떨어뜨리고, 핫스팟 통신사 DNS 는 NXDOMAIN 을 30분 부정 캐시한다(맥 DNS 에 1.1.1.1 추가 + mDNSResponder HUP 로 해결).
- 09-11 **실데이터 전환 + 창 밖 더미 제거(사용자 결정)** — Pi `.env FEED_SOURCE=vision`·`ROLLUP_WINDOW=lunch`(백업 `.env.bak-20260911-1435`), api·vision·admin 재시작, mock 시절 `insights.db` 는 `.bak-20260911-1435` 로 비켜 둠. 카메라 노드는 이제 **수집 창 안에서만 기록하고 창 밖에는 아무 행도 쓰지 않는다**(`vision/counter.py` 에서 `Simulator` 의존 제거, 판정은 순수 함수 `vision/schedule.should_record` + 테스트). 표본이 끊기면 `/api/status` 는 120초 뒤 `no_data` 이고 화면은 '더미데이터' 대신 **'지금은 급식 시간이 아닙니다 · 다음 창'** 띠와 히어로 문구를 띄운다(창 안인데 끊기면 '카메라 표본이 끊겼습니다'). '더미데이터' 문구는 스테이징 mock 출처일 때만 남는다. sw v22. 남은 실무: 관리 화면 구역 탭에서 보정 4점·ROI·λ선(없으면 λ=0 → '배식 시작 대기').
- 09-11 **급식실 현장 진단(맥북) — Wi‑Fi 는 정상, 학교 망이 Tailscale 컨트롤플레인을 막는다** — 맥을 `kjhs_meal`(WPA2 Personal, 채널 36 5 GHz)에 붙여 확인: Pi 는 09:40 부팅 3초 뒤 `kjhs_meal` 에 붙어 `172.31.98.59/23` 을 받았고 서비스 4개(api·vision·admin·cloudflared) active, Cloudflare 터널 인천 4연결 → **`https://kjhs-meal.com` 은 학교 망에서도 200**. "tailnet 에 안 올라옴"의 실체는 `controlplane.tailscale.com` 이 Pi·맥 양쪽에서 000(망 차단) — 목록에 offline 으로 보일 뿐 Pi 는 살아 있고, 같은 LAN 의 맥은 캐시된 키로 직결(`tailscale ping rsp` 9 ms, 관리 앱 `https://rsp.taild5f11e.ts.net:8443` 200). `rsp.local`(mDNS)은 이 망에서 안 잡힘 → 현장 접속은 `ssh -i ~/.ssh/id_ed25519_mealboard xparapx@172.31.98.59` 또는 SSH 터널 `-L 8101:127.0.0.1:8101`. **밖에서도 통한다**: 맥을 핫스팟으로 옮기자 `tailscale ping rsp` 가 학교 공인 IP 로 직결(홀펀칭)됐고 SSH(`100.103.201.77`, `-o HostKeyAlias=rsp`)로 배포까지 됨 — 키를 이미 교환한 기기는 컨트롤플레인 없이도 붙는다(새 기기 등록·키 갱신만 막힘). 정보부에 컨트롤플레인(443) 해제 요청은 그대로. 프로필 수정(WPA3 등)은 불필요.
- 09-11 **Pi 급식실 이전 · Wi-Fi 미접속(진행 중)** — 09-09 Pi 재부팅으로 카메라 재인식(케이블은 전원 끄고 꽂기, Pi 5 는 `poweroff` 뒤 전원 버튼을 눌러야 켜진다). 교체한 카메라도 커널이 `imx708`(표준판)로 보고 — Wide 는 `imx708_wide`. 보드는 Pi 5 16 GB. 급식실 AP `kjhs_meal` 프로필(WPA2-PSK, 우선순위 20)을 미리 등록하고 이전했으나 **tailnet 에 올라오지 않음**(휴대폰은 같은 AP 에 붙음). 진단 절차는 CLAUDE.md §0 '현장 Wi-Fi 진단 절차' — 맥북 세션이 현장에서 `rsp.local` 로 붙어 수행. 주 작업 PC 는 집 PC(Remote Control), 학교 PC 는 예비로 정리(`c1beab9`).
- 09-04 **저녁 — Cloudflare Tunnel 전환(도메인 `kjhs-meal.com`)** — 도메인 검토: 무료 하위 도메인(kro.kr)은 NS 위임이 없어 Cloudflare 등록 불가, 학교 홈페이지 `kongjugo.cnehs.kr` 은 교육청 소유라 학교가 레코드를 못 넣고 하위 위임은 Enterprise 전용, DigitalPlat(`*.dpdns.org`, 180일 갱신)은 가능하나 정식 주소가 아님 → **Cloudflare Registrar 에서 `kjhs-meal.com` 구매**(원가, 자동 갱신 off 가능). NS 가 Cloudflare(carioca·roman)로 잡힌 것 확인. 터널 `mealboard` 생성 후 토큰을 Pi `.env CF_TUNNEL_TOKEN` 에 넣는 과정에서 두 번 실패: nano 오른쪽 클릭은 아무것도 안 들어갔고, `read -s` 는 터미널 제어 문자(`^V`·`ESC[`·끝의 `^D\0\0`)가 값에 섞여 `Provided Tunnel token is not valid`. base64 만 남기고 JSON 닫는 괄호 뒤를 잘라 재인코딩하자 등록 성공 — **인천(icn) 엣지 4연결, 유닛 enable**. 매뉴얼 STEP 9 를 Cloudflare 기준으로 다시 씀(정리 명령·형식 검사 수록, Funnel 은 접힌 예비 절, `funnel reset` 금지 유지), QR 은 새 주소로 재생성(`static/icons/qr.png`). 경로(옛 Public Hostname)는 새 UI 의 '경로 추가 → 게시된 애플리케이션' — 처음엔 유형이 HTTPS 로 저장돼 502(저널 `tls: first record does not look like a TLS handshake`), HTTP 로 고치자 **`https://kjhs-meal.com` 에서 `/api/status`·화면·QR 200, `/api/admin/*` 404(8101 미매핑)** 확인. 하위 도메인 없이 루트로 저장된 것을 그대로 채택(짧다), QR·문서를 루트 주소로 통일. 도메인 공개 직후부터 `/app/.env` 류 스캐너 봇 요청이 저널에 보인다 — 전부 404, 정상. **남은 것**: 휴대폰 셀룰러·학교 Wi‑Fi 확인, Cloudflare SSL/TLS → Always Use HTTPS 켜기(지금은 `http://` 도 그대로 열린다).
- 09-04 **낮 — 문구 손질 · FEED_SOURCE 단일 스위치(사용자 지적)** — 12:10 에 창이 열렸는데 띠는 '더미', 밀집도는 '표본 없음'이라 앞뒤가 안 맞았다: vision 이 창 안에서 업무 공간 실측(0명, 바닥 좌표 없음)을 쓰고 있었기 때문. → `.env FEED_SOURCE` 를 단일 스위치로: `mock` 이면 vision 도 창 안에서 `Simulator` 더미를 기록(밀집도·마커 나옴), `vision` 일 때만 창 안 실측(`6e0b1f9`). 더미 곡선은 창이 열리는 순간 0 명에서 실시간 속도로 진행(첫 봉우리 25분). 문구: 띠 '급식실 설치 전 시험 운영 중', '최근 30분 표본 없음', '메뉴 빈도 TOP 5'·'2회 이상 나온 메뉴 아직 없음'(화면 기본·API reason·도면 셋 다), sw v21(`b0669bc`). mock 유지 상태에서도 관리 화면 실사·메타·구역 편집은 된다(vision 이 카메라를 쥐고 있고 FEED_SOURCE 는 표기·기록 방식만 정한다).
- 09-04 **vision 첫 실사 확인 · 화각 · 관리 화면 오류** — 관리 화면 [실사] 로 카메라 그림 확인(업무 공간, HUD `infer 324ms` ≈ 3fps). 화각이 좁다는 지적 → 두 원인: ① 꽂힌 모듈이 **표준판 imx708(66°)**(Wide 는 `imx708_wide` 로 보고됨, 모듈 미구입) ② `1536x864` 가 센서 중앙 크롭 모드 → 기본 `VISION_SIZE=2304x1296`(센서 전체 2×2 비닝)으로(`204b28f`). 광각은 Wide 모듈을 끼워야만 된다(렌즈 차이, 코드 무관). 관리 화면 `admin.js:297 TypeError` 는 보정 전 vision 이 `floor_xy_norm=null` 을 보내는데 평면도 뷰가 그대로 쓴 것 → 좌표 없는 트랙은 건너뛰고 안내 문구(`fba0fec`). 테스트 166.
- 09-04 **로드맵 ④ vision 프로토타입 가동(`d2debda`)** — 사용자 지적("관리 화면의 실사가 안 된다" → 실사를 내는 프로세스가 없었다; "초점·ROI 재설정은 급식 시간과 무관하게 언제든 돼야 한다")으로 카메라 노드를 만들었다. `vision/counter.py` 가 카메라를 항상 쥐고(배타 자원) ① YOLO(`yolo11n.pt`, ultralytics CPU) + ByteTrack 사람만 → ROI 안 인원 L · λ선 통과율 λ(5분 이동합) · W=L/λ 를 10초마다 `write_sample`(`vision/record.py`, mock 과 공용) ② 메타 소켓 발신 ③ 8102 MJPEG(`vision/debug_stream.py` — 플래그 없거나 10분 초과면 503+삭제, 주석 프레임 960px q70, 뷰어 없으면 인코딩 0). **추론은 수집 창 안 또는 관리자가 실사·메타를 보는 동안만**, 기록은 창 안만 실측·창 밖은 `Simulator` 더미(아무도 안 보면 1fps 로 프레임만 버린다). `vision/counting.py` 는 라인크로싱(부호 변화 + ±buffer_px 완충띠, `out_dir` 은 편집기와 같은 외적 부호)·`RateWindow` 순수 모듈(테스트 6). zones(+local) 은 mtime 으로 2초 안에 리로드. 호모그래피·ROI 가 null 이면 L=화면 안 전원, λ=0(`insufficient_rate`), 구역·타일·positions 건너뜀. `.env` `VIDEO_SOURCE`(picamera|webcam:N|file:) `VISION_SIZE/FPS/IMGSZ/CONF` `YOLO_WEIGHTS`. Pi: `~/captures/yolo11n.pt` → `data/models/`, 전경 실행으로 8102 `/mjpeg` 200 multipart(4초 9프레임 ≈ 3fps) 확인 → `deploy/mealboard-vision.service` 설치, **mock disable → vision enable**(업무 공간 카메라). `FEED_SOURCE=mock` 은 급식실 이전까지 유지. 다음: 관리 화면 구역 탭에서 보정 4점·ROI·λ선. 테스트 166.
- 09-04 **수집 시간창 운영 반영(사용자 요청)** — 파악: 지금까지 시간창은 `LUNCH_START~END`(11:30~14:00) 하나뿐이고 집계·관리 가드에만 쓰였다 — 학년별 점심·석식 창도, 창 밖 수집 중단·더미 표시도 없었다(vision 은 로드맵 ④ 미착수, mock 은 종일 같은 곡선). 반영: `vision/schedule.py`(순수 — `.env MEAL_WINDOWS` 'HH:MM-HH:MM 라벨;…' 파싱, 겹침·역순·오타 거부, `current`·`next_after`) + `app/lunch.py MEALS·meal_now·meal_next`(import 시점 검증; 기본 3학년 점심 11:30~12:30 · 1·2학년 점심 12:30~13:30 · 석식 17:30~18:30), `/api/status` 에 `feed{source, live, now, next}`(live = 출처 vision ∧ 창 안 ∧ 표본 이어짐), 화면 맨 위 `#feedbar` 띠(모든 화면 — core 60초 + 대기시간 화면의 30초 status; 창 밖 "지금은 급식시간이 아닙니다 · 실시간 데이터가 아닌 더미데이터 · 다음 급식 석식 17:30", 창 안인데 mock 이면 "실측 카메라 연결 전"), mock 은 `Simulator`(곡선)·`write_sample`(한 트랜잭션) 로 나누고 창이 열리는 순간 곡선을 0 명에서 다시(창 밖은 같은 곡선을 더미로, 로그에 `[더미]`/`[3학년 점심]` 태그). 카메라 노드 계약은 PLAN §4.4 첫 항목: 창 밖 추론 중단 + `Simulator` 더미. `LUNCH_START~END` 는 집계 창으로 그대로(석식은 집계 밖 — 실측 뒤 판단). Pi 카메라는 업무 공간에 연결됨(AP·공유기 뒤 급식실 이동). sw v20.
- 09-04 **밤 마무리 2 — 관리·주간식단 손질** — 관리 화면: 로그 테일·감사 로그를 접기/펼치기(`<details class="fold">`, 40px 셰브론 SVG, 열림 상태는 브라우저 기억, 닫혀 있으면 읽지 않음), 구역 편집기 평면도를 카드 가운데로(`geom` 이 가로 여유가 크면 라벨 여백을 무시). 사용자 화면: 요일 칩 금요일을 세이지 → 스카이블루 `#BFE3F7`(히트맵·주간식단), 오늘 표시는 차콜 채움·링·밑줄을 모두 버리고 요일색 유지 + **숨쉬기 애니메이션**(scale 1→1.035, 2.6초) + **메뉴 글자 빛 훑기**(background-clip:text, 3.2초) — 둘 다 transform/animation 이라 모션 축소 설정에서 자동 정지. 매뉴얼 STEP 0 에 '포트 지도' 절. sw v19.
- 09-04 **hailo-ollama 설치(사용자 결정)** — `hailo_gen_ai_model_zoo_5.3.0_arm64.deb`(주의: `2026_06/` 경로는 HTML 을 돌려준다 — `2026_04/Hailo10/` 이 맞다). 실행 파일 `/usr/bin/hailo-ollama` + manifest 6개(qwen3:1.7b 포함), 설정 파일 없음, 저장소는 `~/.local/share/hailo-ollama/models/blob/sha256_<해시>`. manifest 의 해시가 우리 `Qwen3-1.7B-Instruct.hef` 와 같아 **하드링크로 연결**(2.9 GB 재다운로드 없음) → `/api/tags` 에 qwen3:1.7b, `/api/chat` 응답 2초. 기본 바인딩이 `0.0.0.0:8000`(학교 LAN 노출)이라 유닛 `deploy/hailo-ollama.service` 는 `OLLAMA_HOST=127.0.0.1:8000`, enable 하지 않고 수동 start/stop(타이머와 HAT 경합 회피). 교훈 둘: 실행 파일은 `--help` 없이 바로 서버가 뜬다, `pkill -f hailo-ollama` 는 자기 ssh 세션까지 죽인다(`pidof` 로).
- 09-04 **밤 마무리 손질** — 밀집도 컬러바를 도면 우측 상단("배식대" 글자 높이)으로, 육각 타일 alpha .55~.8 로 낮춰 도면이 비치게, 관리 편집기 구역 라벨도 세로 통로는 90° 회전, 오류 띠에서 브라우저의 무해한 `ResizeObserver loop` 경고 제외(휴대폰 관리 화면에서 보임). 휴대폰에 Tailscale 을 깔자 관리 화면(`:8443`)이 열렸다 — 원인은 휴대폰이 tailnet 에 없었던 것. sw v15. **다음 세션**: 로드맵 ④ vision(Wide 카메라 기준) 또는 Cloudflare 전환, 미결 질문은 CLAUDE.md §0 세션 인계 참조.
- 09-04 **카메라 렌즈 결정** — 아이폰 전면(약 70°)으로 카메라 자리에서 찍은 사진의 우측이 잘렸다. 구석에서 폭 15.55 m 전체는 약 90° 가 필요 → Camera Module 3 표준(66°)은 부족, **Wide(102°)** 로 확정. 왜곡은 4점 호모그래피로 보정. 로드맵 ④ 는 Wide 기준.
- 09-04 **평면도 라벨·plotly 컬러맵(사용자 요청)** — `static/js/colors.js`(plotly 스케일 사본: `SUNSETDARK`·`JET`·`RDBU`, `ramp(scale,t)`·`gradient`·`luma`) 하나에서 색을 받는다: 대기시간 막대·요일×시각 히트맵 = Sunsetdark(클수록 어둡게, 히트맵 셀은 JS 가 inline 색), 밀집도 육각 = Sunsetdark(jet 을 잠깐 썼다가 사용자 요청으로 같은 날 되돌림), 영양소 칩 = RdBu(권장 100% 가운데 흰색, 50%↓ 빨강, 150%↑ 파랑, 글자는 밝기로 검/흰). 급식실 현황·밀집도 두 평면도에 zones.json 구역 이름을 옅은 회색 halo 로(`drawZoneLabels`, 공개 `/api/insight/zones` 의 polygon — 세로 통로는 90° 회전, 배식대 띠 제외), 고정 "통로" 글자 삭제. 컬러바는 평면도 우측 하단 반투명 알약. sw v12.
- 09-04 **HailoRT 5.1.1 → 5.3.0** — Raspberry Pi 저장소는 `h10-hailort` 5.1.1 에서 멈춰 있고 Hailo 는 `hailort` 라는 다른 이름으로 5.3.0 을 공개 경로(`dev-public.hailo.ai/2026_04/Hailo10/`, 로그인 없음)에 배포한다 → 사용자가 5.1.1 계열 7개 제거 + 5.3.0 deb 3개 설치. 드라이버 postinst 의 dkms 빌드가 커널 6.18 의 `del_timer_sync` 제거로 실패 → `monitor.c` 에 `timer_delete_sync` 호환 정의(커뮤니티 PR #52 와 같은 수정) 후 `dkms build/install`, `dpkg --configure -a`. 재부팅 전에는 `Mismatch Driver version 5:3:0 / 5:1:1`(HAT 에 옛 펌웨어) → 재부팅 뒤 Firmware 5.3.0. Python API 는 휠을 venv 에(`uv pip install --python .venv/bin/python …whl`; `/tmp` 은 tmpfs 라 파일은 `data/models/` 에). 5.1.1 컴파일본 YOLO hef·Qwen2.5 hef 모두 5.3.0 에서 열려 06:10 뉴스 작업은 그대로 돈다. 교훈: 모듈 이름은 그대로(`hailo1x_pci`)라 `lsmod` 만으로는 판을 못 가린다 — `hailortcli fw-control identify` 의 Firmware 로 본다. **Qwen3-1.7B-Instruct 로 교체**: `check_llm.py` go(로드 7.4초, 3.6 tok/s, 요약·주입 PASS — 주입 기사에서도 숫자를 지어내지 않음, 컨텍스트 6,000자 OK → `LLM_CONTEXT_CHARS=4800`), 한국어 직접 생성은 여전히 반복 붕괴("기록 기록 기록…"). 실제 피드 **3/3 요약 성공 + Why 전부 생성**(Qwen2.5 는 2/3, Why 없음). 새 버릇 "- Key Point:" 접두어는 파서가 걷는다. `.env LLM_HEF` 교체, Qwen2.5 파일은 예비로 보관.
- 09-04 **Pi 시스템 업그레이드 뒤 Hailo 드라이버 복구** — 사용자가 `apt full-upgrade`(커널 6.18.34 → 6.18.39, tailscale 1.102.3 등 73개) + `dkms` 설치 + 재부팅. 재부팅 뒤 `lsmod` 에 hailo 모듈이 없었다: `h10-hailort-pcie-driver` 는 `/usr/src/hailort-pcie-driver` dkms 소스인데 처음 설치 때 dkms 가 없어 옛 커널 모듈만 남아 있었다. `apt install --reinstall h10-hailort-pcie-driver` 로 새 커널용 빌드·등록(`/lib/modules/6.18.39…/updates/dkms/hailo1x_pci.ko.xz`, `dkms status` installed) → `hailortcli` 인식·LLM 로드 정상. 업그레이드가 끌고 온 불필요 패키지(`ack`·`libfile-next-perl`)와 데스크톱 잔재 40개(`plymouth`·`lpplug-*`·`qt5ct` 등, autoremove 후보)를 정리. 앞으로 커널이 올라가면 dkms 가 자동 빌드한다(CLAUDE.md §5). 결정: 로컬 LLM 은 당분간 1.5B 로 운용, 더 큰 모델은 나중에.
- 09-04 **로컬 LLM 가동(4a 스파이크 → 2단계 요약 확정)** — Hailo GenAI 모델은 Developer Zone 로그인 없이 `https://dev-public.hailo.ai/v<HailoRT>/blob/<모델>.hef` 로 받을 수 있다(README 의 GenAI 모델 zoo). Pi(HailoRT 5.1.1)에 `v5.1.1/blob/Qwen2.5-1.5B-Instruct.hef`(2.36 GB, 학교망 약 1.7 MB/s·23분)를 `data/models/` 에 받고 `.env LLM_HEF`. 실제 API 는 `genai.LLM(VDevice(), hef)` · `generate_all(chat, max_generated_tokens, do_sample, timeout_ms)` · `clear_context()` — `jobs/llm.py` 를 맞췄다(온도 0 = `do_sample=False`, 종료 토큰 `<|im_end|>` 은 stop 으로 끊김). **결과**: 로드 10.9초, RSS 181 MB, 5~9 tok/s, 컨텍스트 6,000자 OK · 8,000자 실패. 첫 판정 0/5 — 원인은 형식이 아니라 **한국어 생성 품질**(문법 붕괴 "사람낡은 사람들에게서", 중국어 혼입, 요약은 영어로 회귀). 반면 영어 요약은 정확하고 주입 문장("PWNED")도 무시했다(지어낸 수치 5.6% 는 숫자 검사가 잡는다). 리포트용 영어 문장은 사실을 지어냈다("Kimchi Stew was the most popular"). **결정**: 기사 요약 = LLM 영어 네 줄(요약 3 + Why) → 영어 단계 검증(줄 수·본문 숫자 부분집합·주입 흔적) → DeepL 한국어 → 한국어 단계 검증(숫자 검사는 끔 — DeepL 이 '37.4 billion' 을 '374억' 으로 바꾼다). 리포트는 `LLM_REPORT=0` 으로 템플릿 유지. HailoRT 5.3.0 이 있어야 하는 Qwen3-1.7B 는 apt 후보가 없어(로컬 deb) 보류. `check_llm.py` 는 새 파이프라인 기준으로 다시 썼다(주입은 '안전한 거부' 도 PASS). **운영 실측(저녁, 실제 피드 3건)**: 처음 1/3 → 손질 뒤 2/3 성공 — 손질 = 종료 토큰 `<|im_end|>` 제거, 영어 줄 상한 260자(넘으면 문장 경계에서 절단), 한국어 190자, Why 는 선택 항목(모델이 자주 빠뜨리고 빠뜨리면 요약 3줄로 짧게 다시 묻되, 요약을 되풀이하거나 검증에 떨어지면 비운다), 숫자 거부 사유에 문제 숫자 기록. 남은 1건은 3,200자로 잘린 본문 밖의 연도(2060)를 모델이 상식으로 써서 거부 — 검증기의 의도. 카드 칩은 "AI 요약 · Qwen2.5-1.5B-Instruct + DeepL". `.env LLM_CONTEXT_CHARS=3200`. 테스트 148.
- 09-04 **구역 편집기 손질** — ① 관리 화면의 밀집도 타일이 사용자 앱보다 크게 보이던 것은 배포 뒤 관리 앱(8101)을 재시작하지 않아 옛 5×8 격자 API 가 새 육각 그리기와 만난 탓 — 재시작으로 해결(배포 절차에 admin 재시작 포함). ② 편집기 캔버스가 커도 평면도 글자는 사용자 앱 크기로: `floor.js geom` 에 글자 배율 상한 `fu = min(u, 1.35)`. ③ 기본 구역을 5개로: 배식대 앞 · **입구쪽 통로**(창가, 이전 '창가 통로') · **출구쪽 통로**(테이블 왼쪽 빈 띠 x≥0.84) · **1구역/2구역 좌석**(y=0.552, 2·3번째 테이블 블록 사이 통로에서 분리). ④ Shift + 핸들 끌기 = 직각 구속(x 를 나누던 이웃은 새 x 로, y 를 나누던 이웃은 새 y 로 따라와 사각형 유지). 테스트 146.
- 09-04 **화면 손질 6건(사용자 요청)** — ① 최근 30분 막대와 요일×시각 히트맵을 **멜론 `#FF7260` 램프**로(`--heat: 255,114,96`, 끝점·"오늘" 라벨은 멜론 잉크). ② 최근 30분 밀집도를 **육각 타일**로: `vision/zones.py` 격자가 5×8 정사각에서 **10×18 육각(pointy-top, 홀수 행 반 칸 오프셋, 타일 ≈ 2.1 m² ≈ 이전의 1/4.6)** 으로 — `cell_of` 는 axial 좌표 cube 반올림, `hex_center` 가 화면과 같은 중심 공식(테스트: 모든 타일 중심의 왕복). `floor.js drawHeat` 는 값 있는 타일만 seaborn **flare** 9단 램프(`uv run --with seaborn` 으로 cmap 을 0~1 에서 표본)로 채우고 벽 밖은 clip, 나머지는 벌집 윤곽 7%. 격자가 바뀌어 옛 `cell_samples` 번호는 30분 창에서 저절로 빠진다. ③ 데스크톱 실시간뷰 재배치: 급식실 현황(1~5칸)과 밀집도(6~12칸)가 **같은 폭의 평면도**(`--stage-w: clamp(340px, 21.5vw, 420px)`, 1920 에서 420×607 — 이전 520 보다 크다)를 나란히, 밀집도의 글(리드 17px·칩 세로 목록·flare 범례·각주)은 평면도 오른쪽, 오늘 리포트는 아래 한 행 전체. 모바일은 그대로 세로(평면도 아래 글). ④ 오늘급식 아이콘 숟가락·포크의 손잡이를 3.4px 알약 선으로, 숟가락 머리는 활성 때 채움. ⑤ 라벨 '실시간' → **'실시간뷰'**(dock·레일·title·문서). ⑥ 이슈피드: 카드 바탕 차콜 + 기사마다 종이색 타일(위 3px 멜론 선), 데스크톱은 제목 18px. sw `CACHE` v11. 캡처(1920·420)로 확인, 테스트 146.
- 09-03 **저녁 — 휴대폰 접속 불통의 원인과 Cloudflare 전환 결정** — 증상: PC(tailnet 직결)는 되고 휴대폰(셀룰러 포함)은 흰 화면/"보안 연결 불가". 조사: ① 서버 로그에 휴대폰 요청이 14:10 이후 한 건도 없음(카카오톡 내장 브라우저는 연결 실패를 빈 페이지로 보여 준다) ② Funnel 주소가 가리키는 Tailscale **도쿄 입구**(103.84.155.153/217)에 학교 PC 에서도 학교의 Pi 에서도 TLS 가 즉시 끊김 ③ Pi `tailscale netcheck` 에서 도쿄·싱가포르 릴레이 무응답, 홍콩만 응답 → 학교 망이 이날 오후부터 Tailscale 릴레이·컨트롤 서버·`api.trycloudflare.com` 을 막았다(15:34 에는 이 PC 의 Tailscale 도 로그아웃됨). Funnel 은 Pi 가 입구 릴레이에 붙어 있어야 외부 요청을 받으므로 휴대폰은 입구까지만 가고 끊긴다. 조치: tailscaled 재시작으로 Pi 를 홍콩 릴레이에 재귀속(`Relay hkg`), Funnel 443 off→on 재등록(입구 DNS 는 여전히 도쿄), Pi 에 cloudflared 2026.8.3 설치(CLAUDE.md 의 "설치 완료" 메모는 틀렸었다), 학교 망에서 **Cloudflare 엣지 7844 포트가 열려 있음**을 확인. **결정(사용자): 공개 경로를 Cloudflare Tunnel 로 전환**, Funnel 은 예비. 준비해 둔 것: `deploy/mealboard-cloudflared.service`(토큰 방식), `.env.example CF_TUNNEL_TOKEN`, `setup_pi.sh` 7단계(설치·토큰 있으면 enable). **집 PC 에서 할 일**: (1) 도메인 준비 후 Cloudflare 계정에 사이트 추가(네임서버 변경) (2) Zero Trust → Networks → Tunnels → Create → Cloudflared → 토큰 복사 (3) Pi `.env` 에 `CF_TUNNEL_TOKEN=…` 한 줄(채팅·커밋에 적지 않는다) → `sudo systemctl enable --now mealboard-cloudflared` (4) 터널의 Public Hostname 에 `meal.<도메인>` → `http://127.0.0.1:8100`(8101 은 절대 매핑 금지) (5) 휴대폰(셀룰러·학교 Wi‑Fi 둘 다)에서 새 주소 확인 → QR 재생성(매뉴얼 STEP 9) → 매뉴얼 STEP 9 문단·layout 갱신. 서비스워커 캐시 v10 배포됨.
- 09-03 **오후 개정 — 한 번에 한 화면 · 1920×1080 그리드 · 최근 30분 밀집도** — 사용자 보고(모바일에서 스와이프 때문에 화면이 멋대로 넘어가고 아이콘을 두 번 눌러야 바뀜, 데스크톱은 1920×1080 최적화·화면별 독립 페이지 요청)로 내비게이션 모델을 바꿨다: `core.js go()` 가 이전 화면을 숨기고 활성 화면 하나만 보인다(페이저·스크롤 스파이·`landed`·`spy` 제거), 데스크톱은 화면별 12컬럼 grid(max 1800px). 캐스케이드 함정: `insight.css` 끝의 옛 자리 규칙이 `screens.css` 를 덮어 카드가 겹쳤다 → 자리는 screens.css 한 곳에. 실시간 화면의 '구역별 점유율(최근 4주)'을 **'최근 30분 밀집도'** 로: `vision/zones.py` 격자 5×8 `cell_of/count_by_cell`, `queue.db.cell_samples`(mock 이 samples 와 한 트랜잭션), `/api/insight/density?minutes=30`(queue.db 즉석, 셀별 합계·구역 점유율), `floor.js drawHeat`(값이 클수록 진한 틸). 그 밖에 사용자 요청: 추이 그래프 1분 막대(값이 클수록 진하게), 구역 이름 라벨 제거·입구 글자 이동, 관리 origin 에서 공개 평면도가 차콜로 물들던 `.stage` 충돌(`.sstage`), `/api/history` 10초 묶음(0.5MB → 18KB)과 히어로 우선 렌더, mock `--speed 2`(5초 주기 더미), 흰 화면 진단용 오류 띠. 휴대폰 흰 화면은 미해결 — 서버는 정상 응답(200/304), 휴대폰 쪽 오류 문장을 기다린다. 테스트 146.
- 09-03 **Phase 5 문서** — 매뉴얼 STEP 16(5화면 프론트: core.js·room.js·floor.js) · 17(관리 앱: auth·server·stream·meta·zones_store·유닛·sudoers·Serve 명령·터널 진입) · 18(로컬 LLM: llm·report·templates·타이머·newsbody·translators) 추가, 코드 블록은 `check_manual.py --update` 로 63개 동기화. layout.html 에 여섯 번째 "관리 화면" 절(색 규칙·카드 표·데스크톱 배치)과 네비게이션 표 6행. README 구조 트리·CLAUDE.md §0·§3 갱신. 확장 계획 §6 은 4a(스파이크, `.hef` 필요)만 남았다 — 그 스크립트 `check_llm.py`(①import ②로드 ③프롬프트 5종 PASS/FAIL ④YOLO 공존 ⑤컨텍스트 상한 ⑥요약표, ≥4/5 → exit 0)는 미리 써 두었다: `.hef` 를 받으면 학교 Pi 에서 `uv run python check_llm.py --hef …` 한 줄.
- 09-03 **Phase 4c 기사 본문 + LLM 요약** — `jobs/newsbody.py`(전략 순서는 `news_feeds.json` 의 `body`: `rss_content`(content:encoded) → `guardian_api`(키 있을 때) → `html`(<article>/<main> 의 <p>, 스크립트·aside·nav 제거, 40자 미만 단락 버림, 6,000자 상한은 앞 단락 + 마지막 단락) → `description`; 본문은 **메모리에서만**), `jobs/translators.py`(DeepL 분리 + `TRANSLATOR=deepl|local|none`, local 은 제목만·검증), `fetch_news.py` 는 수집 → 본문 → LLM digest(`{bullets 3 ≤90자, why ≤60자}` 검증: 한국어·길이·**본문에 없는 숫자 금지**) → 실패·LLM 없음이면 DeepL 도입부 → 원문 영어, `news.json` 에 `engine`(llm|deepl|none)·`model`·`items[].digest` 추가, `--dry-run`. 카드는 2f 에서 이미 digest 를 그린다. PC dry-run: 실제 피드 3곳이 html 전략으로 4.5~6천 자 확보(Yale 은 TLS 타임아웃 — 그 매체만 건너뜀), LLM 없음·키 없음이라 엔진 none. 매뉴얼 STEP 12 의 "본문을 긁어 요약하지 않는다" 문장을 이 결정으로 개정(저작권 원칙 명시). 테스트 140. **다음: Phase 5 문서**(STEP 16~18, layout, README 구조, CLAUDE.md), 4a 는 `.hef` 확보 후.
- 09-03 **Phase 4b 리포트** — `jobs/llm.py`(LocalLLM 컨텍스트 매니저: `LLM_HEF` 비면·파일 없으면·`hailo_platform` 없으면 `LLMUnavailable`, busy 는 `LLMBusy`; 검증기 `valid_korean` = 한글 비율·URL/개행/마커·길이·**출력 숫자 ⊆ 입력 숫자**), `jobs/report_templates.py`(규칙 문장), `jobs/report.py`(`reports.db` 유일 writer — preview 는 meal.json + 같은 요일 4주 `lunch_bins` 피크, recap 은 `lunch_days`+events; LLM → JSON 파싱 → 검증 → 실패·미설치·busy 60초×3 는 템플릿), `mealboard-report.timer` 05:45·14:20. 4a 스파이크는 학교 Pi 의 `.hef` 가 있어야 해서 뒤로 — 그때까지 모든 리포트는 `engine=template`. 교훈: 숫자 부분집합 검증의 출처에서 **날짜·요일을 빼야** 한다(`2026-09-03` 의 `09` 가 `9` 로 정규화돼 지어낸 "9분"이 통과했다). Pi: 타이머 등록, 첫 실행으로 recap 행 1개 → `/api/insight/text` ok(실시간 화면 리포트 카드). 테스트 133. **다음: 4c**(기사 본문 + LLM 요약, DeepL 폴백).
- 09-03 **Phase 3 말 리뷰(`/code-review high` + 보안 관점, 614bb69..HEAD)** — 9관점 finder → 14후보 검증 → 전부 확인, 전부 반영. 보안: ① SSH 터널의 `?key=` 가 uvicorn 접근 로그 → 저널 → 관리 화면 로그 테일에 그대로 남았다(CLAUDE.md §5 위반) → 유닛 `--no-access-log` + 로그 테일에서 `key=` 마스킹; ② 교차 사이트 검사가 변경 요청에만 있어 tailnet 기기 단위 신원으로 다른 사이트의 `<img>`·EventSource 가 실사·메타를 받을 수 있었다 → GET 포함 모든 게이트 요청에 `Sec-Fetch-Site` 검사; ③ 프레임 JSON 값이 타일 innerHTML 에 이스케이프 없이 들어갔다 → `esc`. 정합성: ④ 실사 뷰어 1명 게이트가 `await` 사이에 두 요청을 통과시켰다 → `claim()` 을 await 전에; ⑤ 릴레이가 멈춘 upstream 에서 영원히 `read()` 에 묶여 끄기가 안 먹었다 → `wait_for` 1초 + off `Event`; ⑥ 워치독의 TCP 포워딩 판정이 실제 JSON 키 형태(포트만)와 달라 절대 잡지 못했다(픽스처가 틀린 형태였다) → 포트로 대조; ⑦ lockdown 이면 whoami 도 403 이라 잠금 배너가 절대 안 떴다 → 403 본문의 reason 으로 표시; ⑧ 학교 Pi 에서 꺼 둔 mock 의 재시작 버튼이 vision 을 끄고 mock 을 켤 수 있었다(Conflicts) → `UnitFileState=disabled` 면 409 + 버튼 없음; ⑨ 편집기가 병합 문서 **전체**를 local 에 써서 첫 저장 뒤 템플릿이 영영 가려지고 VERSION 을 올리면 모든 읽기가 죽었다 → **템플릿과 다른 키만** 저장(version 은 절대 안 씀); ⑩ 터널 첫 진입 `/?key=` 가 쿠키를 못 받고 프로브가 '없음' 을 세션에 기억했다 → 루트에서도 쿠키, `key=` 있으면 다시 프로브; ⑪ 서버가 끄면(다른 관리자·자동) 클라이언트가 실사 모드에 남아 마지막 프레임이 계속 보였다 → 상태 off 면 즉시 메타로; ⑫ `#admin` 으로 열면 #wait 로 튕기고 스파이·키 6 이 관리 화면을 몰랐다 → core `wanted`·`observe()`·ORDER 길이 키; ⑬ 동시 SSE 구독 둘이 같이 bind → Lock; ⑭ 호모그래피 오차 초과 결과를 문서에 넣던 것 → 넣지 않음. 부수: audit 쓰기를 루프 밖 스레드로(`synchronous=NORMAL`), 플래그 until 은 mtime+10분 상한, upstream Content-Type 검사 + nosniff, 스트림 라우트 async 회귀 테스트. 범위 밖 메모는 PLAN §7. 테스트 122.
- 09-03 **Phase 3d 구역 편집기** — `app/admin/zones_store.py`: 읽기는 `load_zones`(템플릿 + `zones.local.json` overlay) 그대로, 쓰기는 `validate_zones` 를 통과한 문서 전체를 **`data/zones.local.json` 에만** `.tmp→os.replace`(템플릿 `zones.json` 은 절대 쓰지 않는다 — 'Pi 는 git pull 만'), 직전 판은 `.bak-<시각>` 최신 5개 보존, `updated_at/by` 서버가 찍음. `/api/admin/zones` GET(overlay 결과 + local/백업 정보) · PUT(422 = 검증 문장 리스트, 감사 행) · `POST zones/homography {pairs×4}`(image_to_floor + 역행렬 + 재투영 오차). 관리 화면 `zones-editor.js`(별도 모듈): 평면도 모드 = 구역 폴리곤(칩 선택, 핸들 드래그 r6/히트 44, 꼭짓점 추가·삭제, 구역 추가·삭제, id·이름) + 메타 스트림의 바닥 점 겹쳐 그리기(정렬 확인); 프레임 모드 = ROI(λ 변은 변을 눌러 지정, 출구 방향 뒤집기, 완충띠 표시) + 보정 4점(이미지 점 찍기 + 바닥 m 입력·도면 프리셋 → 호모그래피 → 바닥 구역을 프레임 위에 점선으로) + '프레임 잡기'(실사 `<img>` 를 캔버스에 복사, 브라우저 메모리뿐). 더티 표시·되돌리기, 편집 중이면 30분 재읽기 건너뜀. 부수: 관리 화면은 등록 직후 `MB.poll("admin", true)` — 부팅 폴링 뒤에 등록되어 카드가 30초 동안 비어 있던 것. 검증: PUT 200(+bak, 두 번째부터), `lambda_edge:[0,2]` 422, GET 왕복, 템플릿 미변경, 415; 화면에서 꼭짓점 추가→저장→ROI λ 변 포인터 지정→보정 4점→호모그래피(오차 0)→저장까지 서버 문서에 반영. 테스트 119. **다음: Phase 3 말 `/code-review high` + `/security-review`**, 그다음 Phase 4.
- 09-03 **Phase 3b Pi 반영 · Phase 3c 스트림** — 3b `68116b2` 를 Pi 에 반영: `mealboard-admin` 유닛, `/etc/sudoers.d/mealboard`(visudo 통과 뒤 440), `systemd-journal` 그룹, `.env` 의 `ADMIN_USERS`·`ADMIN_LOCAL_KEY`(Pi 에서 생성, 어디에도 적지 않음)·`RUN_DIR`, `tailscale serve --bg --https=8443 http://127.0.0.1:8101` — `serve status` 에 8443 은 tailnet only, `funnel status` 는 443→8100 그대로. 이 PC 에서 `:8443/api/admin/whoami` 200(tailnet 신원), 공개 443 의 `/api/admin/*`·`/admin-ui/*` 는 404. 3c: `app/admin/stream.py` — **메타데이터 허브**는 인증된 SSE 구독자가 있을 때만 소켓을 bind(POSIX `RUN_DIR/meta.sock` AF_UNIX DGRAM, Windows 는 UDP 8103 폴백)하고 마지막 구독자가 떠나면 unlink, 구독당 Queue(5) drop-oldest, 구독 ≤3(4번째 429); **MJPEG opt-in** 은 플래그 파일(`/tmp/debug_on`, JSON `until`) + 자동 off 타이머(≤10분, 재시작 뒤에도 until 을 읽어 이어감), 프록시는 `127.0.0.1:DEBUG_PORT/mjpeg` 바이트를 그대로(뷰어 1명, 플래그 사라지면 1초 안에 끊음), on/off/view/meta 구독이 전부 감사 행. `vision/meta.py`(발신 라이브러리, 프레임 하나 = 데이터그램 하나, 60KB 상한)와 `mock_feed --meta`(합성 투영 5fps, 통과 이벤트 포함, 유닛에 반영). 관리 화면 스트림 패널: 메타/실사/끄기 + 평면도(익명 마커·구역 틴트)/프레임(bbox) 뷰, fps·추론·트랙·ROI·λ·대기 타일, 통과 이벤트 카드(50건, 화면에만), 실사는 `<img>` 에 켤 때만 src·끌 때 비움, 카운트다운 배지, 화면을 떠나면 클라이언트 쪽은 모두 끊음. 검증: on 1분 → 플래그·until, `curl -N stream/meta` 4초 23이벤트, 60초 뒤 플래그 삭제 + `stream.autooff` 감사, mjpeg off→409 / vision 없음→502, 415·422 경계. 교훈 둘: on/off 처럼 `call_later` 를 잡는 엔드포인트는 **async def** 여야 한다(동기 def 는 스레드풀 → 루프 없음 → 500); 임베디드 브라우저에서 `<dialog>` 의 close 이벤트가 `showModal` 뒤에 단 리스너에 오지 않아 확인 대화상자를 form submit(e.submitter) 로 바꿨다. 테스트 114. **다음: Phase 3d**(zones.json 편집기).
- 09-03 **Phase 2d~2f + 리뷰 · Phase 3a** — 인사이트 카드 9장(대기시간: 히트맵·황금·예보·병목·품질 / 실시간: 구역 점유율(같은 평면도 위 틴트)·리포트 자리 / 주간식단: 영양 추이 스파크라인·TOP5 / 이슈피드: §5.4 요약 레이아웃), 각 카드는 `data-state`+`.empty` 로 no_data 여도 이유를 말하고 `MB.screens.<화면>.render(cardId, json)` 으로 픽스처 검사. Phase 2 말 `/code-review high`(8관점 → 14후보 → 10건 확인)를 `614bb69` 로 반영: **데스크톱 카드가 390px 에 묶여 보드가 두 줄 띠였던 것**(리뷰 전 캡처를 잘못 읽었다), 숨은 카드 때문에 레일·키·스파이가 죽던 것, 스와이프 뒤 세로 위치, 옛 오류 문구 잔존, fail 뒤 곡선 부활, 숨은 카드의 그리드 구멍, SW 가 502 를 캐시하던 것, off 히어로 모서리 잘림(:has 제거), 리포트 칩·`.why` 스타일. 정리: 두 박자 폴링(라이브 30초 / 집계 30분)으로 요청 수 감소, 평면도는 크기 변경 때만 그림, 공용 헬퍼(core.setState 등). 3a `af59826` 관리자 신원 판정·점심 가드 순수 로직(테스트 13). **다음: Phase 3b**(관리 앱 본체).
- 09-03 **Phase 2a~2c — 프론트 분리·카드 통합·5화면** (PLAN §6) — 2a `a468254` `index.html` 762줄 → 셸 100줄 + `css/base·screens` + `js/core·floor·wait·room·week·today·news`(ES 모듈, 빌드 없음), sw v8, `/js/` `/css/` 에 `no-cache`; 헤드리스 Edge 캡처가 분리 전과 픽셀 동일(시계 값 제외). 2b `bd198ba` 대기시간 카드 = 히어로+추이 한 상자, 오늘급식 카드 하단에 탄소 절, `floor.js` 공용(구역 틴트 `drawZones` 포함). 2c `ae1f051` **5화면 해시 라우터** — 라벨 확정 대기시간·실시간·주간식단·오늘급식·이슈피드, 모바일은 문서 자체가 가로 스냅 페이저 + 하단 dock(SVG 스프라이트, 숟가락+포크 나란히), 데스크톱은 좌측 레일 + 12컬럼 보드, 화면 모듈 `screen={mount,every,poll,fail,activate,deactivate}` 로 보이는 화면만 폴링(실시간 화면 밖 `/api/positions` 0회), 스크롤 스파이·키보드 1~5·ResizeObserver, 도면 layout.html 네비게이션 절. 교훈: 화면 모듈 최상위에서 core 도구를 부르면 순환 import 의 TDZ — `mount()` 로; 헤드리스 Chromium 은 500px 최소 창(420 캡처는 우측 잘림), 미리보기 패널은 fixed 요소를 1680 폭으로 잰다. **Pi 반영(1b~2c)**: `/opt/mealboard`=`d4fbe9b`, rollup 유닛·타이머(14:10) 설치(uv sync 는 건너뜀 — venv 의 ultralytics 보존), `.env` `ROLLUP_WINDOW=all`·`FEED_SOURCE=mock`, 첫 집계 실행(menu_stats 15). **다음: Phase 2d**(대기시간 화면 인사이트 카드).
- 09-03 **Phase 1 — 집계 DB와 인사이트 API** (PLAN §6 1a~1d) — 순수 모듈 `app/lunch.py`(급식 시간창·5분 구간)·`vision/zones.py`(구역 판정·검증·호모그래피, numpy 없음. `zones.json` 위에 미추적 `zones.local.json` overlay)와 `queue.db.zone_samples`(구역별 **인원수만**, mock 이 `samples` 와 같은 트랜잭션에 기록). `app/insight_calc.py` 가 판정 규칙(커버리지 = STALE 초과 공백, 황금 = ok∧대기≤3∧λ≥0.5 5분 연속, 병목 = 줄≥5∧(산출불가∨λ<0.5·평소) 3표본·2분, 상승 속도, 메뉴 정제, 인기 지수)을 담고, `jobs/rollup.py` 가 `insights.db` 의 유일한 writer(날짜 단위 DELETE→INSERT 한 트랜잭션, `user_version`, 잠금, `--all` 백업+되돌리기 명령, `--dry-run`). `/api/insight/{heatmap,day,menus,forecast,quality,nutrition,zones,text}` 8종은 읽기 전용이고 파일이 없으면 `no_data`+`reason`(오늘은 queue.db 즉석 `live`). `mealboard-rollup.timer` 14:10. Phase 말 `/code-review high`(8관점 → 15후보 검증 → 14건 확인)를 전부 반영: `insights.db` 는 **한 파일 = 한 창·한 출처**(`meta` 표, 불일치 시 rollup 거부), 오늘은 창이 열려 있으면 집계 행 대신 live(반쪽 행이 화면을 얼리지 않게), live 는 한 질의로 표본·커버리지 같은 창, 예보 곡선은 자기 날 제외, 잠금 파일 제거(SQLite 직렬화로 충분), 백업은 backup API, `.env` 값은 시작 시 검증, 날짜 인자는 달력 검증(422), 빈 DB 파일은 없는 것으로, mock 은 구역 정의가 깨져도 계속, `T24:00:00` 제거, meal.json 읽기는 `app/mealjson.py` 한 곳. 테스트 82개. 매뉴얼 STEP 15. 검증은 4배속 `mock --scenario stall` 로 병목 2.1분·황금 12분이 이벤트에 잡히는 것까지. **다음: Phase 2a**(static 파일 분리, sw v8).
- 09-03 **확장 계획 확정 + 규칙 개정 + 첫 코드리뷰** — `docs/PLAN-2026-09.md`(6단계: 규칙 → 집계 DB·`/api/insight/*` → 5화면 프론트(모바일 하단 dock `#wait #room #week #today #news`, 데스크톱 보드+레일) → tailnet 전용 관리 앱(8101, Serve 8443, 허용목록, 메타데이터 SSE + MJPEG opt-in) → 로컬 LLM(Hailo GenAI) 리포트·기사 본문 요약 → 문서). CLAUDE.md §0·§2·§3·§5 개정(파일당 쓰기 주체 하나, 포트, 승인된 디버그 예외, 뉴스 행), `data/zones.json` 을 git 템플릿으로 추적하고 Pi 보정값은 미추적 `zones.local.json` overlay. 화면: 평면도 벽 모서리 채움, 마커를 노란 스마일(테두리 없음, 20% 축소)로 바꾸고 점멸은 **위 겹 캔버스 `#marks` + CSS opacity(1.4초, 0.55~1)** — 리뷰에서 90ms 전체 재그리기 루프·30초마다 번쩍임·서버 단절 시 옛 마커 계속 깜박임을 잡아 교체. 이 PC(jh-home) SSH 키를 Pi `authorized_keys` 에 추가(`ssh mbpi`). **다음: Phase 1a**(`app/lunch.py`·`vision/zones.py`·`zone_samples`·mock 구역 카운트·테스트) — PLAN §6 표 참조.
- 09-02 **급식실 현황을 실측 탑뷰 2D 로 확정** — 8/28 의 가상 평면도를 실측·실배치 기반으로 전면 교체. 하루 동안 2.5D 아이소메트릭(쿼터뷰) 두 방향과 three.js 실시간 3D 까지 시도한 끝에, 가독성 우선으로 탑뷰 평면(세로형 318:460)에 정착. 확정 배치: 배식대 위(카메라는 그 오른쪽 끝)·퇴식구(싱크대)→출구 좌측·대기통로(창가) 우측·입구는 통로 끝단. 테이블 6열×[6인×3|6인×2|6인×4+4인], 의자는 도면 기호식(테이블 밑 수납형). 좌표계(원점 배식구∧통로 구석, 0~1 정규화)는 zones.json·mock·향후 calibrate 와 공유. 부수 수확: `drawChart` 의 `Math.max(...배열)` 스택오버플로(고배속 mock 에서 수십만 행) 수정. `CACHE` v7.
- 09-02 **학교 급식실 실측·구조 확정** — 폭 15.55m × 길이 24.65m × 천장 2.68m(앱 측정). 실사 3장 + 도면 스케치로 배식대/퇴식구/출구/통로/입구 위치 확정. 카메라(CM3 표준판, 수평 화각 약 66°)는 배식대 오른쪽 끝에서 통로 방향 — 2304×1296 캡처 + imgsz 640 이면 10~12m 까지 안정 탐지.
- 09-02 **Pi 학교 이설 + AI HAT 가동** — 학교 와이파이 프로필(NetworkManager) 사전 등록으로 이설 직후 tailnet 자동 복귀(SSID 가 같아도 다른 AP·다른 망 모두 무관, 인터넷만 나가면 됨). AI HAT 은 `lspci` 로 **Hailo-10H**(VLM 지원 세대) 확인, `hailo-h10-all 5.1.1` 설치·`hailortcli identify` 검증. 급식실에 AP 가 없어 재이설 대기 — AP 설치 예정. 부팅은 콘솔(멀티유저) 유지, SSH 운용.
- 09-01 **YOLO 사전 탑재 + 원격 시험** — 학교 PC SSH 키 등록(`xparapx@rsp`), ultralytics 8.4.137 을 `/opt/mealboard/.venv` 에 임시 설치(uv pip — pyproject 미반영, 로드맵 ④ 때 정식 편입). CM3 카메라 캡처 → yolo11n CPU 추론 250ms/장 → picamera2+ByteTrack 실시간 추적 MJPEG(127.0.0.1 전용, SSH 터널 열람)까지 검증. `classes=[0]`(사람만)은 속도 이득이 아니라 트래커 오염 방지용임을 실측으로 확인.

### 2026-08
- 08-31 **배포가 브라우저에 도달하지 않던 진짜 원인** — `StaticFiles` 가 `Cache-Control` 을 붙이지 않아, 브라우저가 `Last-Modified` 로 신선도를 추측한다(묵은 기간의 10%). 이틀 된 `index.html` 은 약 5시간 동안 서버에 묻지도 않는다. 08-29 에 서비스워커를 네트워크 우선으로 고치고도 증상이 남았던 이유 — 그 fetch 자체가 디스크 캐시로 채워졌다. 화면·`sw.js`·`manifest.json` 에 `Cache-Control: no-cache`(= 쓰기 전 반드시 검사, ETag 같으면 304) 미들웨어를 두고, 서비스워커의 화면 fetch 도 `cache:"no-cache"` 로. `CACHE` 는 v6.
- 08-31 좌측 컬럼 손질 — 히어로·추이 카드 높이 +15%(여백과 안쪽 간격으로), 평면도는 방(외곽)이 카드 상자를 그대로 채우고 **안쪽 설비 크기는 가로 폭만으로** 재도록 다시 그린다(카드가 세로로 길어져도 배식대 두께가 변하지 않는다 — 이전에는 폭·높이 보정을 가로세로 모두 같은 함수로 계산해 세로로 늘어났다). 퇴식구는 출구 틈 아래에서 배식대 아랫변까지 이어지는 세로 띠로. 영양소 칩은 ±% 의 부호를 색으로 — 음수는 붉은 계열 그라데이션, EAR 미만만 멜론 외곽선 + "부족". `docs/layout.html`·매뉴얼 동반 갱신.
- 08-29 화면 확정 — 위치 마커 스마일, 이번 주 식단을 요일 컬럼 + 요일 5색으로, 긴 메뉴명 줄바꿈, 데스크톱 두 패널 아래 끝 맞춤(짧은 쪽 마지막 카드가 흡수하고 평면도는 그만큼 커진다). 이슈 링크는 해외 기후 매체 4곳(Carbon Brief·Inside Climate News·The Guardian·Yale Climate Connections) + 피드 요약 + DeepL 한국어 번역. `docs/layout.html` 도면 v5 동반 갱신.
- 08-29 `static/sw.js` 서비스워커 재작성 — 화면은 네트워크 우선, `activate` 에서 옛 캐시 삭제 + `clients.claim()`, `skipWaiting()`. v1 은 캐시 이름이 고정이고 화면까지 캐시 우선이라 **배포해도 브라우저에 도달하지 않았다**(v5 배포에서 실제로 겪음).
- 08-29 주말·공휴일(`today` 가 null)에도 이번 주 식단 카드를 보여주도록 수정. 오늘 강조는 캐시된 `today.date` 가 아니라 브라우저 시계 기준.
- 08-29 **대시보드 v5** — 전 카드의 1.5px 외곽선을 걷어내고 라운드 24 + 2단 그림자로 재설계(팔레트 3색은 유지). 네 가지 추가: ① 알레르기 표시(NEIS 가 메뉴명에 붙여 보내던 번호를 분리, 내 항목은 브라우저에만 저장) ② 이번 주 식단(`meal.json` 의 `week[]` — 서버 변경 없음) ③ 배식대 도착 시각 ④ 평소 곡선(`/api/typical` 신설, 같은 요일 우선·표본 부족 시 최근 7일 폴백). 뺀 것: 잔반 자기신고 4버튼과 개인 월간 누적, 영양소 막대 6색(색 검증 전 항목 탈락 → 상태 2색 칩으로 대체). 도면 v5(`docs/layout.html`)·매뉴얼 STEP 14 동반 갱신.
- 08-29 개발 PC → Pi SSH 키 로그인 구성(`mbpi` 별칭), `.env.example` 에 `PI_USER`·`PI_HOST` 추가.
- 08-28 대시보드 전 구간 완료 — 영양 지표 3종·영양소 막대, 잔반 탄소 카드, 익명 위치 마커(평면도), 팔레트 v4·고도/모션 체계·데스크톱 2컬럼 — 와 Tailscale Funnel 공개(고정 주소) 완료. 남은 것: vision(카메라 카운팅)과 학교 Pi 이전·실측.
- 08-28 매뉴얼 STEP 11~13(탄소·이슈 링크·위치 마커) 추가, `check_manual.py` 로 코드 블록 ↔ 파일 동기화 자동화.
- 08-27 저장소 뼈대, systemd 유닛·setup_pi.sh, Little's law 함수+테스트, SQLite 스키마·API 3종, mock_feed, NEIS 급식·영양 지표.

## 세션 인계 이력 (CLAUDE.md §0 에서 09-12 이관 — 당시 기록 원문)

- **세션 인계(09-11 밤, 학교 PC·핫스팟 → 다음은 집 PC)**: 저장소 `3587535`, Pi 동일. 실측 운영 중(FEED_SOURCE=vision, 보정 4점·ROI·λ 저장, 수집 창 11:20/12:30/17:00). 오늘 밤 끝난 것 — 석식 NEIS·주간 2행·오늘 2열·알레르기 띠, 편집기 직관성·템플릿 초기화, 더미 표본 정리(queue.db 165만 행 → 백업 `queue.db.bak-20260911-182139`), 인사이트 빈 상태 문구, **배포 뒤 옛 모듈 잔류 대책 둘**(관리 UI `?v=UI_VERSION`+`no-store`, 학생 화면은 워커 교체 시 자동 재로드 — `core.UI_VERSION` 과 `sw.js CACHE` 는 화면이 바뀔 때 같이 올린다, 지금 v27).
  **다음**: ① 월요일(9/14) 11:20 첫 실측 점심 — L·λ·평면도 마커 확인, 마커가 밀리면 보정 y(4 m) 조정 ② 14:10 첫 집계 뒤 히트맵 셀 확인 ③ 안정되면 `queue.db.bak-*`(200 MB)·`.env.bak-*` 정리 ④ 석식을 집계 창에 넣을지, 영양 지표를 두 끼 기준으로 할지(영양 교사 판단) ⑤ Wide 카메라 오면 재보정. 학교 PC 에서는 `ssh mbcf` 도 학교 망에서 막힌다 — 핫스팟 또는 집 PC.

- **세션 인계(09-11, 학교 PC → 집 PC 로 넘김)**: 저장소 최신은 `git log -1`. 09-09~11 에 한 것 — ① Pi 재부팅으로 카메라 케이블 재인식(케이블은 전원 끄고 꽂기, Pi 5 는 poweroff 뒤 전원 버튼 필요) ② 교체한 모듈도 커널이 `imx708`(표준판)로 보고 — Wide 는 `imx708_wide`·`imx708_wide.json` 이어야 한다, 화각 실측(1 m 에서 가로 1.3 m=66°, 2.4 m=102°)로 정품 여부 판단 ③ 보드는 Pi 5 16 GB(리비전 `e04171`) ④ Pi 를 급식실로 이전, Wi-Fi 프로필 `kjhs_meal`(WPA2-PSK, 우선순위 20) 등록 — **09-11 오후 현장 진단 결과(맥북): Wi‑Fi 는 정상**(Pi 는 `kjhs_meal` WPA2 에 붙어 `172.31.98.59/23`, 서비스 4개 active, `https://kjhs-meal.com` 200). 안 올라온 이유는 학교 망이 `controlplane.tailscale.com`(443)을 막기 때문(Pi·맥 모두 000) — 절차 ① 두 번째 갈래. 현장에서는 `rsp.local` 이 안 잡히니 `ssh -i ~/.ssh/id_ed25519_mealboard xparapx@172.31.98.59` 로, 관리 앱은 같은 LAN 에서 `https://rsp.taild5f11e.ts.net:8443`(캐시 키 직결) 또는 SSH 터널 `-L 8101:127.0.0.1:8101`. **관리 앱 외부 접속 = `https://admin.kjhs-meal.com`(09-11 저녁 결정, 매뉴얼 9-5)**: 학교 망이 DERP 릴레이도 막아 Tailscale 직결은 NAT 구멍이 살아 있는 동안만 통한다(14:40 됐다가 14:57 끊김) → 급식실 밖에서 관리 앱을 열려면 Cloudflare Access 경로가 정식. **16:15 완료**: Access 앱 `admin`(대상 `admin.kjhs-meal.com`+`ssh.kjhs-meal.com`, 정책 `admins`=이메일 허용목록, 로그인 Cloudflare 계정 + One-time PIN), 팀 도메인 `empty-voice-fb65.cloudflareaccess.com`, 터널 경로 `admin`→HTTP 127.0.0.1:8101 · `ssh`→SSH localhost:22. Pi `.env` 에 `CF_ACCESS_TEAM`·`CF_ACCESS_AUD`(백업 `.env.bak-20260911-1615`), `uv sync`(pyjwt 2.13), admin 재시작. 검증: 위조 `cf-ray`+Tailscale 헤더 → 403 no_token, 토큰 없이 → 302, 토큰으로 whoami → `via: access`. **SSH 는 이제 `ssh mbcf`**(맥 `~/.ssh/config`: `ssh.kjhs-meal.com`, ProxyCommand `cloudflared access ssh`, HostKeyAlias rsp; 첫 사용 때 브라우저 로그인, 토큰 24시간) — 학교 망과 무관. 다른 PC 도 cloudflared 설치 + 같은 항목이면 된다.
  **오늘 겪은 함정 두 개**: ① 학교 안 어떤 Wi‑Fi(게이트웨이 192.168.1.1)는 `admin.`·`ssh.` 하위 도메인의 TLS ClientHello 를 SNI 로 떨어뜨린다(`kjhs-meal.com` 루트는 통과) → 학교 안에서는 LAN 직결 `rsp.taild5f11e.ts.net:8443`, 밖에서는 Cloudflare. ② 휴대폰 핫스팟(통신사 DNS)은 새 하위 도메인을 NXDOMAIN 으로 30분 부정 캐시한다 → 맥 Wi‑Fi DNS 에 `1.1.1.1` 을 넣고 `sudo killall -HUP mDNSResponder` 로 로컬 캐시를 비운다.
  **밖에서도 Tailscale 은 통한다(09-11 14:40 실증, 단 NAT 구멍이 살아 있는 동안만)**: 맥을 휴대폰 핫스팟으로 옮긴 뒤 `tailscale ping rsp` 가 학교 공인 IP 로 직결(홀펀칭)됐고 `ssh xparapx@100.103.201.77`(known_hosts 는 `-o HostKeyAlias=rsp`)로 pull·restart 까지 됨 — 이미 키를 교환한 기기끼리는 컨트롤플레인 없이도 붙는다. 못 하는 것은 새 기기 등록·키 갱신·MagicDNS 갱신뿐. 집 PC 도 기존 피어이므로 `ssh mbpi` 가 통할 가능성이 높다(안 되면 IP + HostKeyAlias). 정보부에 컨트롤플레인 해제 요청은 그대로 진행. 프로필 수정 불필요.
  **현장 Wi-Fi 진단 절차(맥북 세션용, 09-11)** — 맥북을 `kjhs_meal` 에 붙이고(휴대폰은 붙었음 → AP·인터넷 정상) 순서대로:
  ① `ssh xparapx@rsp.local` 로 붙어 `nmcli -t -f NAME,DEVICE con show --active` · `nmcli -t -f IP4.ADDRESS,IP4.GATEWAY dev show wlan0` · `curl -s -m 5 -o /dev/null -w '%{http_code}' https://controlplane.tailscale.com/`.
     붙으면 Pi 는 AP 에 있는 것. `kjhs_meal` 활성인데 controlplane 이 000 이면 AP 상위 망이 Tailscale 을 막는 것(정보부에 차단 해제 요청, 그동안 학생 화면은 Cloudflare `kjhs-meal.com` 으로만).
  ② `rsp.local` 이 안 잡히면 Pi 가 AP 에 못 붙은 것 → 이더넷으로 맥북과 직결(USB-C 어댑터) 뒤 같은 이름으로 접속, `journalctl -u NetworkManager -n 40 --no-pager` 에서 kjhs·auth·secret·fail·wpa 줄로 사유 확인.
  ③ 사유가 `secrets were required`·`association took too long`·WPA3 면 `sudo nmcli con modify kjhs_meal wifi-sec.key-mgmt sae && sudo nmcli con up kjhs_meal`. 비밀번호 오타면 `sudo nmcli con modify kjhs_meal wifi-sec.psk '<비밀번호>'`(채팅·커밋에 값 남기지 말 것).
     SSID 가 다르면 `sudo nmcli con modify kjhs_meal 802-11-wireless.ssid '<정확한 SSID>'`. AP 가 5 GHz DFS 채널(52~144)만 쓰면 공유기에서 2.4 GHz 또는 36~48 채널로.
  ④ 붙은 뒤 `tailscale status --self` 가 Running 이면 다른 PC 에서 `ssh mbpi` 가 통한다. 확인: `rpicam-hello --list-cameras`(imx708) · `systemctl is-active mealboard-api mealboard-vision mealboard-admin mealboard-cloudflared` · `curl -s 127.0.0.1:8100/api/status`.
  ⑤ 결과(원인·조치)를 docs/WORKLOG.md 에 한 줄 적고 push. Pi 안의 프로필 변경은 커밋 대상이 아니다.
  **실데이터 전환 완료(09-11 14:35, 맥북에서 LAN 으로)**: Pi `.env FEED_SOURCE=vision`·`ROLLUP_WINDOW=lunch`(백업 `.env.bak-20260911-1435`), api·vision·admin 재시작, mock 시절 `insights.db` 는 `insights.db.bak-20260911-1435` 로 비켜 둠(rollup 타이머가 새로 만든다). **남은 것: 관리 화면 구역 탭에서 보정 4점·ROI·λ선**(없으면 λ=0 이라 '배식 시작 대기'만 나온다) → Cloudflare 공개 주소 `https://kjhs-meal.com` 확인(학교 교직원 망에서는 차단됨 — 휴대폰 셀룰러로).

- **세션 인계(09-04 아침, 학교 PC)**: 저장소·Pi 모두 `fba0fec`. 오늘 아침 끝난 것 — ① 수집 시간창 3개 + 더미데이터 띠(`50b600a`) ② vision 프로토타입 가동, Pi 는 mock → `mealboard-vision`(`d2debda`) ③ 카메라 모드 2304x1296(`204b28f`) ④ 관리 화면 보정 전 null 좌표 가드(`fba0fec`).
  **지금 꽂힌 카메라는 표준판(66°)** — Wide 모듈은 아직 없음(사용자 확인). 광각은 모듈 교체로만 가능. **사용자 작업 방식 갱신**: Pi 반영(pull·restart)도 Claude 가 `ssh mbpi` 로 직접 한다, 코드 조각을 사용자에게 써 달라는 요청은 하지 않는다(설명만).
  **다음**: 관리 화면 구역 탭에서 보정 4점·ROI·λ선(표준판으로 연습, Wide 오면 재보정) → 카메라 앞에서 걸어 λ 통과 확인 → CPU fps 가 모자라면 Hailo hef 백엔드.

- **세션 인계(09-04 밤, 이 PC=jh-home)**: 저장소·Pi 모두 최신(`git log -1` 로 확인). 오늘 끝난 것 — 화면 손질(육각 밀집도·plotly 컬러맵·구역 라벨·컬러바 우측 상단), 기본 구역 5개,
  HailoRT 5.3.0 + Qwen3-1.7B(기사 요약 3/3), 휴대폰 Tailscale 로 관리 화면 접속 확인. **hailo-ollama 설치함(09-04 밤, 사용자 결정)**: `hailo_gen_ai_model_zoo_5.3.0_arm64.deb`(공개 경로 `2026_04/Hailo10/`), 모델 저장소 `~/.local/share/hailo-ollama/models/blob/sha256_<해시>` 에
  `data/models/Qwen3-1.7B-Instruct.hef` 를 하드링크(해시 동일, 재다운로드 없음). 유닛 `deploy/hailo-ollama.service` 는 **enable 하지 않는다** — 필요할 때 `sudo systemctl start hailo-ollama`, 끝나면 stop
  (뉴스·리포트 타이머와 HAT 경합). `OLLAMA_HOST=127.0.0.1:8000` 고정(기본 0.0.0.0 은 학교 LAN 에 열린다), 밖에서는 `ssh -L 8000:127.0.0.1:8000 mbpi`. 파이프라인은 여전히 `jobs/llm.py` 직접 호출.
  **미결 질문(사용자 답 대기)**: ⓑ Plant 규칙 개정 — 사용자는 "포트 8000 은 이제 안 쓴다, Plant 는 GitHub 에 있다" 고 했으나 §0·§2 의 Plant 금지 규칙은 아직 그대로(파일·DB 삭제는 명시 지시 때만).
