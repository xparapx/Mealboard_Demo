# Mealboard — 학교 급식실 대기시간·영양 대시보드

급식실 카메라(라즈베리파이 5 + YOLO)로 대기 인원·처리율을 재어 **예상 대기시간**(Little's law, W = L / λ)을 내고,
NEIS 급식 API 의 메뉴·알레르기·영양 지표(에너지 충족률·적정비율·MAR)와 주간 식단, 잔반 탄소 카드, 익명 위치 마커를 웹 대시보드(PWA)로 보여준다.
학생·교사는 QR 로 접속한다. 영상은 어디에도 저장·전송하지 않고 숫자만 SQLite 에 남긴다.

- 구축 매뉴얼(전 단계·코드 수록): [docs/manual.html](docs/manual.html) · 화면 도면(스펙의 단일 출처): [docs/layout.html](docs/layout.html)
- Claude Code 작업 규칙: [CLAUDE.md](CLAUDE.md)

## 구조

```
app/        FastAPI 공개 앱 — main.py, config.py(.env 단일 출처), db.py, lunch.py(급식 창), insight_calc.py(판정 규칙), insights_db.py(읽기 전용),
            mealjson.py, routers/{status,history,meal,positions,news,typical,insight}.py
app/admin/  관리 앱(별도 프로세스 8101, tailnet 전용) — server.py, auth.py, guard.py, sysctl.py, watchdog.py, audit.py, health.py,
            stream.py(메타 SSE 허브·MJPEG 프록시), zones_store.py(zones.local.json writer), routers/{system,stream,zones}.py, static/{admin.js,zones-editor.js,admin.css}
vision/     waittime.py(Little's law), zones.py(구역·ROI·호모그래피, 순수), meta.py(메타데이터 발신). counter·debug_stream 은 로드맵 ④
jobs/       mock_feed.py(카메라 대역, --meta), fetch_neis.py(→ meal.json), fetch_news.py + newsbody.py + translators.py(→ news.json),
            rollup.py(→ insights.db), report.py + report_templates.py + llm.py(→ reports.db)
static/     index.html(셸) · css/{base,screens,insight}.css · js/{core,floor,wait,room,week,today,news}.js(ES 모듈, 빌드 없음) · manifest.json · sw.js
data/       queue.db·insights.db·reports.db·admin.db·meal.json·news.json·positions.json·zones.local.json (git 제외)
            / nutrition_std.json·carbon_std.json·news_feeds.json·zones.json(템플릿) (포함)
deploy/     mealboard-{api,mock,vision,admin,neis,news,rollup,report}.service, *.timer, sudoers-mealboard, cloudflared-config.yml(견본)
tests/      순수 로직 140개 — waittime·typical·lunch·zones·insight_calc·mealjson·admin_{auth,guard,sysctl,stream,zones}·report·news
docs/       manual.html(구축 매뉴얼) · layout.html(화면 도면) · PLAN-2026-09.md(확장 계획서, §6 진행표)
setup_pi.sh Pi 최초 설치·유닛 갱신 (멱등) · check_manual.py 매뉴얼 코드 블록 ↔ 파일 대조
```

## 셋업 요약

- **개발 PC**: `uv venv && uv sync` → 창 1 `uv run python -m jobs.mock_feed --speed 60`, 창 2 `uv run uvicorn app.main:app --port 8100` → `http://localhost:8100/`. 테스트 `uv run pytest -q`.
- **Pi**: `bash setup_pi.sh` (클론·venv·systemd 유닛). 이후 배포는 `/opt/mealboard` 에서 `git pull` → 해당 서비스 `systemctl restart`.
- **외부 공개**: 스테이징·시범 운영은 Tailscale Funnel(`tailscale funnel --bg 8100`, 고정 주소). 정식 배포 시 Cloudflare Tunnel + 유료 도메인. 자세한 절차는 매뉴얼 STEP 9.

## 작업 로그

### 2026-09
- 09-03 **Phase 5 문서** — 매뉴얼 STEP 16(5화면 프론트: core.js·room.js·floor.js) · 17(관리 앱: auth·server·stream·meta·zones_store·유닛·sudoers·Serve 명령·터널 진입) · 18(로컬 LLM: llm·report·templates·타이머·newsbody·translators) 추가, 코드 블록은 `check_manual.py --update` 로 63개 동기화. layout.html 에 여섯 번째 "관리 화면" 절(색 규칙·카드 표·데스크톱 배치)과 네비게이션 표 6행. README 구조 트리·CLAUDE.md §0·§3 갱신. 확장 계획 §6 은 4a(스파이크, `.hef` 필요)만 남았다.
- 09-03 **Phase 4c 기사 본문 + LLM 요약** — `jobs/newsbody.py`(전략 순서는 `news_feeds.json` 의 `body`: `rss_content`(content:encoded) → `guardian_api`(키 있을 때) → `html`(<article>/<main> 의 <p>, 스크립트·aside·nav 제거, 40자 미만 단락 버림, 6,000자 상한은 앞 단락 + 마지막 단락) → `description`; 본문은 **메모리에서만**), `jobs/translators.py`(DeepL 분리 + `TRANSLATOR=deepl|local|none`, local 은 제목만·검증), `fetch_news.py` 는 수집 → 본문 → LLM digest(`{bullets 3 ≤90자, why ≤60자}` 검증: 한국어·길이·**본문에 없는 숫자 금지**) → 실패·LLM 없음이면 DeepL 도입부 → 원문 영어, `news.json` 에 `engine`(llm|deepl|none)·`model`·`items[].digest` 추가, `--dry-run`. 카드는 2f 에서 이미 digest 를 그린다. PC dry-run: 실제 피드 3곳이 html 전략으로 4.5~6천 자 확보(Yale 은 TLS 타임아웃 — 그 매체만 건너뜀), LLM 없음·키 없음이라 엔진 none. 매뉴얼 STEP 12 의 "본문을 긁어 요약하지 않는다" 문장을 이 결정으로 개정(저작권 원칙 명시). 테스트 140. **다음: Phase 5 문서**(STEP 16~18, layout, README 구조, CLAUDE.md), 4a 는 `.hef` 확보 후.
- 09-03 **Phase 4b 리포트** — `jobs/llm.py`(LocalLLM 컨텍스트 매니저: `LLM_HEF` 비면·파일 없으면·`hailo_platform` 없으면 `LLMUnavailable`, busy 는 `LLMBusy`; 검증기 `valid_korean` = 한글 비율·URL/개행/마커·길이·**출력 숫자 ⊆ 입력 숫자**), `jobs/report_templates.py`(규칙 문장), `jobs/report.py`(`reports.db` 유일 writer — preview 는 meal.json + 같은 요일 4주 `lunch_bins` 피크, recap 은 `lunch_days`+events; LLM → JSON 파싱 → 검증 → 실패·미설치·busy 60초×3 는 템플릿), `mealboard-report.timer` 05:45·14:20. 4a 스파이크는 학교 Pi 의 `.hef` 가 있어야 해서 뒤로 — 그때까지 모든 리포트는 `engine=template`. 교훈: 숫자 부분집합 검증의 출처에서 **날짜·요일을 빼야** 한다(`2026-09-03` 의 `09` 가 `9` 로 정규화돼 지어낸 "9분"이 통과했다). Pi: 타이머 등록, 첫 실행으로 recap 행 1개 → `/api/insight/text` ok(실시간 화면 리포트 카드). 테스트 133. **다음: 4c**(기사 본문 + LLM 요약, DeepL 폴백).
- 09-03 **Phase 3 말 리뷰(`/code-review high` + 보안 관점, 614bb69..HEAD)** — 9관점 finder → 14후보 검증 → 전부 확인, 전부 반영. 보안: ① SSH 터널의 `?key=` 가 uvicorn 접근 로그 → 저널 → 관리 화면 로그 테일에 그대로 남았다(CLAUDE.md §5 위반) → 유닛 `--no-access-log` + 로그 테일에서 `key=` 마스킹; ② 교차 사이트 검사가 변경 요청에만 있어 tailnet 기기 단위 신원으로 다른 사이트의 `<img>`·EventSource 가 실사·메타를 받을 수 있었다 → GET 포함 모든 게이트 요청에 `Sec-Fetch-Site` 검사; ③ 프레임 JSON 값이 타일 innerHTML 에 이스케이프 없이 들어갔다 → `esc`. 정합성: ④ 실사 뷰어 1명 게이트가 `await` 사이에 두 요청을 통과시켰다 → `claim()` 을 await 전에; ⑤ 릴레이가 멈춘 upstream 에서 영원히 `read()` 에 묶여 끄기가 안 먹었다 → `wait_for` 1초 + off `Event`; ⑥ 워치독의 TCP 포워딩 판정이 실제 JSON 키 형태(포트만)와 달라 절대 잡지 못했다(픽스처가 틀린 형태였다) → 포트로 대조; ⑦ lockdown 이면 whoami 도 403 이라 잠금 배너가 절대 안 떴다 → 403 본문의 reason 으로 표시; ⑧ 학교 Pi 에서 꺼 둔 mock 의 재시작 버튼이 vision 을 끄고 mock 을 켤 수 있었다(Conflicts) → `UnitFileState=disabled` 면 409 + 버튼 없음; ⑨ 편집기가 병합 문서 **전체**를 local 에 써서 첫 저장 뒤 템플릿이 영영 가려지고 VERSION 을 올리면 모든 읽기가 죽었다 → **템플릿과 다른 키만** 저장(version 은 절대 안 씀); ⑩ 터널 첫 진입 `/?key=` 가 쿠키를 못 받고 프로브가 '없음' 을 세션에 기억했다 → 루트에서도 쿠키, `key=` 있으면 다시 프로브; ⑪ 서버가 끄면(다른 관리자·자동) 클라이언트가 실사 모드에 남아 마지막 프레임이 계속 보였다 → 상태 off 면 즉시 메타로; ⑫ `#admin` 으로 열면 #wait 로 튕기고 스파이·키 6 이 관리 화면을 몰랐다 → core `wanted`·`observe()`·ORDER 길이 키; ⑬ 동시 SSE 구독 둘이 같이 bind → Lock; ⑭ 호모그래피 오차 초과 결과를 문서에 넣던 것 → 넣지 않음. 부수: audit 쓰기를 루프 밖 스레드로(`synchronous=NORMAL`), 플래그 until 은 mtime+10분 상한, upstream Content-Type 검사 + nosniff, 스트림 라우트 async 회귀 테스트. 범위 밖 메모는 PLAN §7. 테스트 122.
- 09-03 **Phase 3d 구역 편집기** — `app/admin/zones_store.py`: 읽기는 `load_zones`(템플릿 + `zones.local.json` overlay) 그대로, 쓰기는 `validate_zones` 를 통과한 문서 전체를 **`data/zones.local.json` 에만** `.tmp→os.replace`(템플릿 `zones.json` 은 절대 쓰지 않는다 — 'Pi 는 git pull 만'), 직전 판은 `.bak-<시각>` 최신 5개 보존, `updated_at/by` 서버가 찍음. `/api/admin/zones` GET(overlay 결과 + local/백업 정보) · PUT(422 = 검증 문장 리스트, 감사 행) · `POST zones/homography {pairs×4}`(image_to_floor + 역행렬 + 재투영 오차). 관리 화면 `zones-editor.js`(별도 모듈): 평면도 모드 = 구역 폴리곤(칩 선택, 핸들 드래그 r6/히트 44, 꼭짓점 추가·삭제, 구역 추가·삭제, id·이름) + 메타 스트림의 바닥 점 겹쳐 그리기(정렬 확인); 프레임 모드 = ROI(λ 변은 변을 눌러 지정, 출구 방향 뒤집기, 완충띠 표시) + 보정 4점(이미지 점 찍기 + 바닥 m 입력·도면 프리셋 → 호모그래피 → 바닥 구역을 프레임 위에 점선으로) + '프레임 잡기'(실사 `<img>` 를 캔버스에 복사, 브라우저 메모리뿐). 더티 표시·되돌리기, 편집 중이면 30분 재읽기 건너뜀. 부수: 관리 화면은 등록 직후 `MB.poll("admin", true)` — 부팅 폴링 뒤에 등록되어 카드가 30초 동안 비어 있던 것. 검증: PUT 200(+bak, 두 번째부터), `lambda_edge:[0,2]` 422, GET 왕복, 템플릿 미변경, 415; 화면에서 꼭짓점 추가→저장→ROI λ 변 포인터 지정→보정 4점→호모그래피(오차 0)→저장까지 서버 문서에 반영. 테스트 119. **다음: Phase 3 말 `/code-review high` + `/security-review`**, 그다음 Phase 4.
- 09-03 **Phase 3b Pi 반영 · Phase 3c 스트림** — 3b `68116b2` 를 Pi 에 반영: `mealboard-admin` 유닛, `/etc/sudoers.d/mealboard`(visudo 통과 뒤 440), `systemd-journal` 그룹, `.env` 의 `ADMIN_USERS`·`ADMIN_LOCAL_KEY`(Pi 에서 생성, 어디에도 적지 않음)·`RUN_DIR`, `tailscale serve --bg --https=8443 http://127.0.0.1:8101` — `serve status` 에 8443 은 tailnet only, `funnel status` 는 443→8100 그대로. 이 PC 에서 `:8443/api/admin/whoami` 200(tailnet 신원), 공개 443 의 `/api/admin/*`·`/admin-ui/*` 는 404. 3c: `app/admin/stream.py` — **메타데이터 허브**는 인증된 SSE 구독자가 있을 때만 소켓을 bind(POSIX `RUN_DIR/meta.sock` AF_UNIX DGRAM, Windows 는 UDP 8103 폴백)하고 마지막 구독자가 떠나면 unlink, 구독당 Queue(5) drop-oldest, 구독 ≤3(4번째 429); **MJPEG opt-in** 은 플래그 파일(`/tmp/debug_on`, JSON `until`) + 자동 off 타이머(≤10분, 재시작 뒤에도 until 을 읽어 이어감), 프록시는 `127.0.0.1:DEBUG_PORT/mjpeg` 바이트를 그대로(뷰어 1명, 플래그 사라지면 1초 안에 끊음), on/off/view/meta 구독이 전부 감사 행. `vision/meta.py`(발신 라이브러리, 프레임 하나 = 데이터그램 하나, 60KB 상한)와 `mock_feed --meta`(합성 투영 5fps, 통과 이벤트 포함, 유닛에 반영). 관리 화면 스트림 패널: 메타/실사/끄기 + 평면도(익명 마커·구역 틴트)/프레임(bbox) 뷰, fps·추론·트랙·ROI·λ·대기 타일, 통과 이벤트 카드(50건, 화면에만), 실사는 `<img>` 에 켤 때만 src·끌 때 비움, 카운트다운 배지, 화면을 떠나면 클라이언트 쪽은 모두 끊음. 검증: on 1분 → 플래그·until, `curl -N stream/meta` 4초 23이벤트, 60초 뒤 플래그 삭제 + `stream.autooff` 감사, mjpeg off→409 / vision 없음→502, 415·422 경계. 교훈 둘: on/off 처럼 `call_later` 를 잡는 엔드포인트는 **async def** 여야 한다(동기 def 는 스레드풀 → 루프 없음 → 500); 임베디드 브라우저에서 `<dialog>` 의 close 이벤트가 `showModal` 뒤에 단 리스너에 오지 않아 확인 대화상자를 form submit(e.submitter) 로 바꿨다. 테스트 114. **다음: Phase 3d**(zones.json 편집기).
- 09-03 **Phase 2d~2f + 리뷰 · Phase 3a** — 인사이트 카드 9장(대기시간: 히트맵·황금·예보·병목·품질 / 실시간: 구역 점유율(같은 평면도 위 틴트)·리포트 자리 / 주간식단: 영양 추이 스파크라인·TOP5 / 이슈피드: §5.4 요약 레이아웃), 각 카드는 `data-state`+`.empty` 로 no_data 여도 이유를 말하고 `MB.screens.<화면>.render(cardId, json)` 으로 픽스처 검사. Phase 2 말 `/code-review high`(8관점 → 14후보 → 10건 확인)를 `614bb69` 로 반영: **데스크톱 카드가 390px 에 묶여 보드가 두 줄 띠였던 것**(리뷰 전 캡처를 잘못 읽었다), 숨은 카드 때문에 레일·키·스파이가 죽던 것, 스와이프 뒤 세로 위치, 옛 오류 문구 잔존, fail 뒤 곡선 부활, 숨은 카드의 그리드 구멍, SW 가 502 를 캐시하던 것, off 히어로 모서리 잘림(:has 제거), 리포트 칩·`.why` 스타일. 정리: 두 박자 폴링(라이브 30초 / 집계 30분)으로 요청 수 감소, 평면도는 크기 변경 때만 그림, 공용 헬퍼(core.setState 등). 3a `af59826` 관리자 신원 판정·점심 가드 순수 로직(테스트 13). **다음: Phase 3b**(관리 앱 본체).
- 09-03 **Phase 2a~2c — 프론트 분리·카드 통합·5화면** (PLAN §6) — 2a `a468254` `index.html` 762줄 → 셸 100줄 + `css/base·screens` + `js/core·floor·wait·room·week·today·news`(ES 모듈, 빌드 없음), sw v8, `/js/` `/css/` 에 `no-cache`; 헤드리스 Edge 캡처가 분리 전과 픽셀 동일(시계 값 제외). 2b `bd198ba` 대기시간 카드 = 히어로+추이 한 상자, 오늘급식 카드 하단에 탄소 절, `floor.js` 공용(구역 틴트 `drawZones` 포함). 2c `ae1f051` **5화면 해시 라우터** — 라벨 확정 대기시간·실시간·주간식단·오늘급식·이슈피드, 모바일은 문서 자체가 가로 스냅 페이저 + 하단 dock(SVG 스프라이트, 숟가락+포크 나란히), 데스크톱은 좌측 레일 + 12컬럼 보드, 화면 모듈 `screen={mount,every,poll,fail,activate,deactivate}` 로 보이는 화면만 폴링(실시간 화면 밖 `/api/positions` 0회), 스크롤 스파이·키보드 1~5·ResizeObserver, 도면 layout.html 네비게이션 절. 교훈: 화면 모듈 최상위에서 core 도구를 부르면 순환 import 의 TDZ — `mount()` 로; 헤드리스 Chromium 은 500px 최소 창(420 캡처는 우측 잘림), 미리보기 패널은 fixed 요소를 1680 폭으로 잰다. **Pi 반영(1b~2c)**: `/opt/mealboard`=`d4fbe9b`, rollup 유닛·타이머(14:10) 설치(uv sync 는 건너뜀 — venv 의 ultralytics 보존), `.env` `ROLLUP_WINDOW=all`·`FEED_SOURCE=mock`, 첫 집계 실행(menu_stats 15). **다음: Phase 2d**(대기시간 화면 인사이트 카드).
- 09-03 **Phase 1 — 집계 DB와 인사이트 API** (PLAN §6 1a~1d) — 순수 모듈 `app/lunch.py`(급식 시간창·5분 구간)·`vision/zones.py`(구역 판정·검증·호모그래피, numpy 없음. `zones.json` 위에 미추적 `zones.local.json` overlay)와 `queue.db.zone_samples`(구역별 **인원수만**, mock 이 `samples` 와 같은 트랜잭션에 기록). `app/insight_calc.py` 가 판정 규칙(커버리지 = STALE 초과 공백, 황금 = ok∧대기≤3∧λ≥0.5 5분 연속, 병목 = 줄≥5∧(산출불가∨λ<0.5·평소) 3표본·2분, 상승 속도, 메뉴 정제, 인기 지수)을 담고, `jobs/rollup.py` 가 `insights.db` 의 유일한 writer(날짜 단위 DELETE→INSERT 한 트랜잭션, `user_version`, 잠금, `--all` 백업+되돌리기 명령, `--dry-run`). `/api/insight/{heatmap,day,menus,forecast,quality,nutrition,zones,text}` 8종은 읽기 전용이고 파일이 없으면 `no_data`+`reason`(오늘은 queue.db 즉석 `live`). `mealboard-rollup.timer` 14:10. Phase 말 `/code-review high`(8관점 → 15후보 검증 → 14건 확인)를 전부 반영: `insights.db` 는 **한 파일 = 한 창·한 출처**(`meta` 표, 불일치 시 rollup 거부), 오늘은 창이 열려 있으면 집계 행 대신 live(반쪽 행이 화면을 얼리지 않게), live 는 한 질의로 표본·커버리지 같은 창, 예보 곡선은 자기 날 제외, 잠금 파일 제거(SQLite 직렬화로 충분), 백업은 backup API, `.env` 값은 시작 시 검증, 날짜 인자는 달력 검증(422), 빈 DB 파일은 없는 것으로, mock 은 구역 정의가 깨져도 계속, `T24:00:00` 제거, meal.json 읽기는 `app/mealjson.py` 한 곳. 테스트 82개. 매뉴얼 STEP 15. 검증은 4배속 `mock --scenario stall` 로 병목 2.1분·황금 12분이 이벤트에 잡히는 것까지. **다음: Phase 2a**(static 파일 분리, sw v8).
- 09-03 **확장 계획 확정 + 규칙 개정 + 첫 코드리뷰** — `docs/PLAN-2026-09.md`(6단계: 규칙 → 집계 DB·`/api/insight/*` → 5화면 프론트(모바일 하단 dock `#wait #room #week #today #news`, 데스크톱 보드+레일) → tailnet 전용 관리 앱(8101, Serve 8443, 허용목록, 메타데이터 SSE + MJPEG opt-in) → 로컬 LLM(Hailo GenAI) 리포트·기사 본문 요약 → 문서). CLAUDE.md §0·§2·§3·§5 개정(파일당 쓰기 주체 하나, 포트, 승인된 디버그 예외, 뉴스 행), `data/zones.json` 을 git 템플릿으로 추적하고 Pi 보정값은 미추적 `zones.local.json` overlay. 화면: 평면도 벽 모서리 채움, 마커를 노란 스마일(테두리 없음, 20% 축소)로 바꾸고 점멸은 **위 겹 캔버스 `#marks` + CSS opacity(1.4초, 0.55~1)** — 리뷰에서 90ms 전체 재그리기 루프·30초마다 번쩍임·서버 단절 시 옛 마커 계속 깜박임을 잡아 교체. 이 PC(jh-home) SSH 키를 Pi `authorized_keys` 에 추가(`ssh mbpi`). **다음: Phase 1a**(`app/lunch.py`·`vision/zones.py`·`zone_samples`·mock 구역 카운트·테스트) — PLAN §6 표 참조.
- 09-02 **급식실 현황을 실측 탑뷰 2D 로 확정** — 8/28 의 가상 평면도를 실측·실배치 기반으로 전면 교체. 하루 동안 2.5D 아이소메트릭(쿼터뷰) 두 방향과 three.js 실시간 3D 까지 시도한 끝에, 가독성 우선으로 탑뷰 평면(세로형 318:460)에 정착. 확정 배치: 배식대 위(카메라는 그 오른쪽 끝)·퇴식구(싱크대)→출구 좌측·대기통로(창가) 우측·입구는 통로 끝단. 테이블 6열×[6인×3|6인×2|6인×4+4인], 의자는 도면 기호식(테이블 밑 수납형). 좌표계(원점 배식구∧통로 구석, 0~1 정규화)는 zones.json·mock·향후 calibrate 와 공유. 부수 수확: `drawChart` 의 `Math.max(...배열)` 스택오버플로(고배속 mock 에서 수십만 행) 수정. `CACHE` v7.
- 09-02 **학교 급식실 실측·구조 확정** — 폭 15.55m × 길이 24.65m × 천장 2.68m(앱 측정). 실사 3장 + 도면 스케치로 배식대/퇴식구/출구/통로/입구 위치 확정. 카메라(CM3 표준판, 수평 화각 약 66°)는 배식대 오른쪽 끝에서 통로 방향 — 2304×1296 캡처 + imgsz 640 이면 10~12m 까지 안정 탐지.
- 09-02 **Pi 학교 이설 + AI HAT 가동** — 학교 와이파이 프로필(NetworkManager) 사전 등록으로 이설 직후 tailnet 자동 복귀(SSID 가 같아도 다른 AP·다른 망 모두 무관, 인터넷만 나가면 됨). AI HAT 은 `lspci` 로 **Hailo-10H**(VLM 지원 세대) 확인, `hailo-h10-all 5.1.1` 설치·`hailortcli identify` 검증. 급식실에 AP 가 없어 재이설 대기 — AP 설치 예정. 부팅은 콘솔(멀티유저) 유지, SSH 운용.
- 09-01 **YOLO 사전 탑재 + 원격 시험** — 학교 PC SSH 키 등록(`xparapx@rsp`), ultralytics 8.4.137 을 `/opt/mealboard/.venv` 에 임시 설치(uv pip — pyproject 미반영, 로드맵 ④ 때 정식 편입). CM3 카메라 캡처 → yolo11n CPU 추론 250ms/장 → picamera2+ByteTrack 실시간 추적 MJPEG(127.0.0.1 전용, SSH 터널 열람)까지 검증. `classes=[0]`(사람만)은 속도 이득이 아니라 트래커 오염 방지용임을 실측으로 확인.

### 2026-08
- 08-31 **배포가 브라우저에 도달하지 않던 진짜 원인** — `StaticFiles` 가 `Cache-Control` 을 붙이지 않아, 브라우저가 `Last-Modified` 로 신선도를 추측한다(묵은 기간의 10%). 이틀 된 `index.html` 은 약 5시간 동안 서버에 묻지도 않는다. 08-29 에 서비스워커를 네트워크 우선으로 고치고도 증상이 남았던 이유 — 그 fetch 자체가 디스크 캐시로 채워졌다. 화면·`sw.js`·`manifest.json` 에 `Cache-Control: no-cache`(= 쓰기 전 반드시 검사, ETag 같으면 304) 미들웨어를 두고, 서비스워커의 화면 fetch 도 `cache:"no-cache"` 로. `CACHE` 는 v6.
- 08-31 좌측 컬럼 손질 — 히어로·추이 카드 높이 +15%(여백과 안쪽 간격으로), 평면도는 방(외곽)이 카드 상자를 그대로 채우고 **안쪽 설비 크기는 가로 폭만으로** 재도록 다시 그린다(카드가 세로로 길어져도 배식대 두께가 변하지 않는다 — 이전에는 폭·높이 보정을 가로세로 모두 같은 함수로 계산해 세로로 늘어났다). 퇴식구는 출구 틈 아래에서 배식대 아랫변까지 이어지는 세로 띠로. 영양소 칩은 ±% 의 부호를 색으로 — 음수는 붉은 계열 그라데이션, EAR 미만만 멜론 외곽선 + "부족". `docs/layout.html`·매뉴얼 동반 갱신.
- 08-29 화면 확정 — 위치 마커 스마일, 이번 주 식단을 요일 컬럼 + 요일 5색으로, 긴 메뉴명 줄바꿈, 데스크톱 두 패널 아래 끝 맞춤(짧은 쪽 마지막 카드가 흡수하고 평면도는 그만큼 커진다). 이슈 링크는 해외 기후 매체 4곳(Carbon Brief·Inside Climate News·The Guardian·Yale Climate Connections) + 피드 요약 + DeepL 한국어 번역. `docs/layout.html` 도면 v5 동반 갱신.
- 08-29 `static/sw.js` 서비스워커 재작성 — 화면은 네트워크 우선, `activate` 에서 옛 캐시 삭제 + `clients.claim()`, `skipWaiting()`. v1 은 캐시 이름이 고정이고 화면까지 캐시 우선이라 **배포해도 브라우저에 도달하지 않았다**(v5 배포에서 실제로 겪음).
- 08-29 주말·공휴일(`today` 가 null)에도 이번 주 식단 카드를 보여주도록 수정. 오늘 강조는 캐시된 `today.date` 가 아니라 브라우저 시계 기준.
- 08-29 **대시보드 v5** — 전 카드의 1.5px 외곽선을 걷어내고 라운드 24 + 2단 그림자로 재설계(팔레트 3색은 유지). 네 가지 추가: ① 알레르기 표시(NEIS 가 메뉴명에 붙여 보내던 번호를 분리, 내 항목은 브라우저에만 저장) ② 이번 주 식단(`meal.json` 의 `week[]` — 서버 변경 없음) ③ 배식대 도착 시각 ④ 평소 곡선(`/api/typical` 신설, 같은 요일 우선·표본 부족 시 최근 7일 폴백). 뺀 것: 잔반 자기신고 4버튼과 개인 월간 누적, 영양소 막대 6색(색 검증 전 항목 탈락 → 상태 2색 칩으로 대체). 도면 v5(`docs/layout.html`)·매뉴얼 STEP 14 동반 갱신.
- 08-29 개발 PC → Pi SSH 키 로그인 구성(`mbpi` 별칭), `.env.example` 에 `PI_USER`·`PI_HOST` 추가.
- 08-28 대시보드 전 구간 완료 — 영양 지표 3종·영양소 막대, 잔반 탄소 카드, 익명 위치 마커(평면도), 팔레트 v4·고도/모션 체계·데스크톱 2컬럼 — 와 Tailscale Funnel 공개(고정 주소) 완료. 남은 것: vision(카메라 카운팅)과 학교 Pi 이전·실측.
- 08-28 매뉴얼 STEP 11~13(탄소·이슈 링크·위치 마커) 추가, `check_manual.py` 로 코드 블록 ↔ 파일 동기화 자동화.
- 08-27 저장소 뼈대, systemd 유닛·setup_pi.sh, Little's law 함수+테스트, SQLite 스키마·API 3종, mock_feed, NEIS 급식·영양 지표.
